---
phase: 45-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/ui/index.html
  - worker/ui/modules/project-picker.js
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Browser tab shows 'Ligamen — Service Dependency Graph' (no 'AllClear' in page title)"
    - "Toolbar h1 reads 'Ligamen Service Graph' (no 'AllClear' visible in rendered UI)"
    - "Empty-state message references '/ligamen:map', not '/allclear:map'"
  artifacts:
    - path: "worker/ui/index.html"
      provides: "Graph UI HTML with updated title and toolbar header"
      contains: "Ligamen — Service Dependency Graph"
    - path: "worker/ui/modules/project-picker.js"
      provides: "Project picker with updated slash command reference"
      contains: "/ligamen:map"
  key_links:
    - from: "worker/ui/index.html"
      to: "browser tab"
      via: "<title> tag"
      pattern: "Ligamen — Service Dependency Graph"
    - from: "worker/ui/index.html"
      to: "toolbar"
      via: "<h1> tag"
      pattern: "Ligamen Service Graph"
    - from: "worker/ui/modules/project-picker.js"
      to: "empty-state message"
      via: "innerHTML assignment"
      pattern: "/ligamen:map"
---

<objective>
Rename all visible "AllClear" branding in the graph UI to "Ligamen".

Purpose: Phase 45 of the v4.0 Ligamen Rebrand milestone — after this plan no AllClear text appears in the browser tab, the toolbar header, or the project-picker empty-state message.
Output: Updated worker/ui/index.html and worker/ui/modules/project-picker.js with Ligamen branding throughout.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename AllClear to Ligamen in index.html</name>
  <files>worker/ui/index.html</files>
  <read_first>worker/ui/index.html</read_first>
  <action>
Make exactly two string replacements in worker/ui/index.html:

1. Line 6 — change the page title:
   FROM: `<title>AllClear — Service Dependency Graph</title>`
   TO:   `<title>Ligamen — Service Dependency Graph</title>`

2. Line 419 — change the toolbar h1 text:
   FROM: `<h1>AllClear Service Graph</h1>`
   TO:   `<h1>Ligamen Service Graph</h1>`

No other lines change. Do not alter CSS, layout, or any other content.
  </action>
  <verify>
    <automated>grep -n "AllClear" /Users/ravichillerega/sources/allclear/worker/ui/index.html | wc -l | grep -q "^0$" && echo "PASS: no AllClear in index.html" || echo "FAIL: AllClear still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AllClear" worker/ui/index.html` returns no matches
    - `grep "Ligamen — Service Dependency Graph" worker/ui/index.html` matches line 6 (title tag)
    - `grep "Ligamen Service Graph" worker/ui/index.html` matches the h1 inside #toolbar
  </acceptance_criteria>
  <done>worker/ui/index.html contains no "AllClear" strings; title tag and h1 both read "Ligamen"</done>
</task>

<task type="auto">
  <name>Task 2: Rename /allclear:map to /ligamen:map in project-picker.js</name>
  <files>worker/ui/modules/project-picker.js</files>
  <read_first>worker/ui/modules/project-picker.js</read_first>
  <action>
Make exactly one string replacement in worker/ui/modules/project-picker.js:

Line 23 — change the no-projects message:
  FROM: `'<p class="no-projects">No projects found. Run <code>/allclear:map</code> to scan your repos first.</p>'`
  TO:   `'<p class="no-projects">No projects found. Run <code>/ligamen:map</code> to scan your repos first.</p>'`

No other lines change.
  </action>
  <verify>
    <automated>grep -n "allclear" /Users/ravichillerega/sources/allclear/worker/ui/modules/project-picker.js | wc -l | grep -q "^0$" && echo "PASS: no allclear in project-picker.js" || echo "FAIL: allclear still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "allclear" worker/ui/modules/project-picker.js` returns no matches
    - `grep "/ligamen:map" worker/ui/modules/project-picker.js` matches the innerHTML assignment on line 23
  </acceptance_criteria>
  <done>project-picker.js empty-state message references /ligamen:map; no allclear strings remain</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full grep check:

```bash
grep -rn "AllClear\|allclear" worker/ui/index.html worker/ui/modules/project-picker.js
```

Expected: zero output (no matches).
</verification>

<success_criteria>
1. `grep -r "AllClear" worker/ui/` returns no matches
2. `grep -r "allclear" worker/ui/index.html worker/ui/modules/project-picker.js` returns no matches
3. `grep "Ligamen — Service Dependency Graph" worker/ui/index.html` returns the title tag line
4. `grep "Ligamen Service Graph" worker/ui/index.html` returns the h1 line
5. `grep "/ligamen:map" worker/ui/modules/project-picker.js` returns the empty-state message line
</success_criteria>

<output>
After completion, create `.planning/phases/45-ui/45-01-SUMMARY.md` following the summary template.
</output>
