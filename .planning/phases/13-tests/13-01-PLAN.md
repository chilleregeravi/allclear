---
phase: 13-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - tests/bats
  - tests/test_helper/bats-support
  - tests/test_helper/bats-assert
  - tests/detect.bats
  - tests/siblings.bats
autonomous: true
requirements:
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "bats test runner is available at tests/bats/bin/bats without any system install"
    - "detect.bats tests pass verifying project type detection for Python, Rust, Node/TS, Go, and mixed-language repos"
    - "siblings.bats tests pass verifying sibling repo discovery from parent directory and exclusion of non-git directories"
  artifacts:
    - path: "tests/bats"
      provides: "bats-core test runner (git submodule)"
    - path: "tests/test_helper/bats-support"
      provides: "bats-support helper (git submodule)"
    - path: "tests/test_helper/bats-assert"
      provides: "bats-assert assertion library (git submodule)"
    - path: "tests/detect.bats"
      provides: "Project type detection tests"
      min_lines: 40
    - path: "tests/siblings.bats"
      provides: "Sibling repo discovery tests"
      min_lines: 30
  key_links:
    - from: "tests/detect.bats"
      to: "lib/detect.sh"
      via: "bash source in each @test"
      pattern: 'source.*lib/detect\.sh'
    - from: "tests/siblings.bats"
      to: "lib/siblings.sh"
      via: "bash source in each @test"
      pattern: 'source.*lib/siblings\.sh'
---

<objective>
Set up the bats test infrastructure (git submodules for bats-core, bats-support, bats-assert) and create the library function tests for detect.sh and siblings.sh.

Purpose: All subsequent test plans depend on bats being available. Library tests are the simplest test type (source + call function + assert) and validate the foundation that all hooks depend on.
Output: Working bats runner, detect.bats and siblings.bats test files.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tests/13-RESEARCH.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bats-core, bats-support, and bats-assert as git submodules</name>
  <files>.gitmodules, tests/bats, tests/test_helper/bats-support, tests/test_helper/bats-assert</files>
  <action>
Add three git submodules from the bats-core organization:

```bash
git submodule add https://github.com/bats-core/bats-core tests/bats
git submodule add https://github.com/bats-core/bats-support tests/test_helper/bats-support
git submodule add https://github.com/bats-core/bats-assert tests/test_helper/bats-assert
```

After adding, verify the runner works:
```bash
tests/bats/bin/bats --version
```

This must output a version string (1.x.x). Do NOT use brew install or any system-level install — the submodule IS the runner.
  </action>
  <verify>
    <automated>tests/bats/bin/bats --version</automated>
  </verify>
  <done>bats --version outputs a version string; .gitmodules contains all three submodule entries; tests/bats/bin/bats is executable</done>
</task>

<task type="auto">
  <name>Task 2: Create detect.bats and siblings.bats library tests</name>
  <files>tests/detect.bats, tests/siblings.bats</files>
  <action>
Create two bats test files that test the shared library functions. Since the library scripts (lib/detect.sh, lib/siblings.sh) may not exist yet (Phase 2 is pending), write tests against the expected API contract from ARCHITECTURE.md. The tests will fail (RED state) until the libraries are implemented — this is correct and expected.

**tests/detect.bats** — Must cover TEST-05:
- setup(): load bats-support and bats-assert, set CLAUDE_PLUGIN_ROOT, create FIXTURES_DIR with mktemp -d
- teardown(): rm -rf FIXTURES_DIR
- Test: "detects Python from pyproject.toml" — touch pyproject.toml in FIXTURES_DIR, source lib/detect.sh, run detect_project_type FIXTURES_DIR, assert_output --partial "python"
- Test: "detects Rust from Cargo.toml" — touch Cargo.toml, assert_output --partial "rust"
- Test: "detects Node/TS from package.json" — touch package.json, assert_output --partial "node" (or "typescript")
- Test: "detects Go from go.mod" — touch go.mod, assert_output --partial "go"
- Test: "detects mixed Python+Node" — touch both pyproject.toml and package.json, assert_output --partial "python" AND assert_output --partial "node"
- Test: "returns empty/unknown for directory with no manifest" — empty FIXTURES_DIR, run detect_project_type, refute_output --partial "python" (verify no false positive)

Use `assert_output --partial` for flexibility — the exact output format of detect_project_type is not locked yet (per RESEARCH.md open question 2). Source the library inside each @test body (not setup) to avoid cross-test contamination (per RESEARCH.md Pitfall 4).

**tests/siblings.bats** — Must cover TEST-06:
- setup(): load helpers, set CLAUDE_PLUGIN_ROOT, create FIXTURES_DIR with mktemp -d
- teardown(): rm -rf FIXTURES_DIR
- Test: "discovers sibling repos from parent dir" — create PARENT/repo-a/.git, PARENT/repo-b/.git, PARENT/not-a-repo (no .git). Source lib/siblings.sh, run discover_siblings (or find_siblings — use whichever name; add a comment noting the function name must match lib/siblings.sh). Assert output contains "repo-b", refute output contains "not-a-repo"
- Test: "excludes the current repo from results" — discover_siblings called from repo-a should not list repo-a itself
- Test: "returns empty when no siblings exist" — create PARENT/only-repo/.git with no other repos. Run discover_siblings from only-repo, assert output is empty or contains no paths
- Test: "handles parent dir with no git repos" — empty PARENT dir, assert graceful handling (no crash, exit 0)

Note: The function name in siblings.sh is not yet finalized (RESEARCH.md open question 1). Use `discover_siblings` as the assumed name and add a comment: "# Function name must match lib/siblings.sh — update if Phase 2 uses a different name".
  </action>
  <verify>
    <automated>tests/bats/bin/bats tests/detect.bats tests/siblings.bats 2>&1 || echo "Tests failed (expected — lib scripts not yet implemented)"</automated>
  </verify>
  <done>Both .bats files exist with correct structure: setup/teardown, load helpers, CLAUDE_PLUGIN_ROOT set, at least 6 tests in detect.bats and 4 tests in siblings.bats. Tests will fail until Phase 2 implements the libraries — this is the expected RED state.</done>
</task>

</tasks>

<verification>
- `tests/bats/bin/bats --version` outputs a version
- `tests/detect.bats` contains @test blocks for Python, Rust, Node/TS, Go, mixed, and empty-dir cases
- `tests/siblings.bats` contains @test blocks for discovery, exclusion, empty, and no-siblings cases
- Both files load bats-support and bats-assert in setup()
- Both files set export CLAUDE_PLUGIN_ROOT in setup()
</verification>

<success_criteria>
Bats runner is available via git submodule. Library test files exist with comprehensive test cases covering TEST-05 and TEST-06 requirements. Tests are in RED state (expected to fail until lib/ scripts are implemented in Phase 2).
</success_criteria>

<output>
After completion, create `.planning/phases/13-tests/13-01-SUMMARY.md`
</output>
