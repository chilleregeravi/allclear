---
phase: 13-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/file-guard.bats
  - tests/session-start.bats
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-08
must_haves:
  truths:
    - "file-guard.bats tests verify exit 2 for writes to .env, .env.local, lock files, credential files, .pem/.key files, and generated directories"
    - "file-guard.bats tests verify stdout contains permissionDecision deny JSON for all hard-blocked files"
    - "file-guard.bats tests verify exit 0 with warning message for SQL migrations, generated code, and CHANGELOG.md"
    - "file-guard.bats tests verify exit 0 with no output for safe files"
    - "session-start.bats tests verify additionalContext JSON appears in stdout"
    - "session-start.bats tests verify exit 0 in all scenarios"
  artifacts:
    - path: "tests/file-guard.bats"
      provides: "Guard hook tests for hard blocks and soft warnings"
      min_lines: 80
    - path: "tests/session-start.bats"
      provides: "Session start hook tests"
      min_lines: 30
  key_links:
    - from: "tests/file-guard.bats"
      to: "scripts/file-guard.sh"
      via: "stdin JSON pipe in bash -c"
      pattern: "bash.*SCRIPT"
    - from: "tests/session-start.bats"
      to: "scripts/session-start.sh"
      via: "stdin JSON pipe in bash -c"
      pattern: "bash.*SCRIPT"
---

<objective>
Create bats tests for the PreToolUse file guard hook (hard blocks + soft warnings) and the SessionStart session-start hook (context injection), covering exit code contracts and JSON output schemas.

Purpose: These tests prove the two most security-critical contracts: (1) PreToolUse guard exits 2 with correct permissionDecision deny JSON for sensitive files (TEST-08), and (2) soft warnings allow writes but surface messages. Session-start tests verify context injection works.
Output: tests/file-guard.bats and tests/session-start.bats
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tests/13-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file-guard.bats with hard-block and soft-warn tests</name>
  <files>tests/file-guard.bats</files>
  <action>
Create tests/file-guard.bats covering TEST-03 (guard hard blocks and soft warnings) and TEST-08 (exit 2 with correct JSON).

**Setup pattern:**
```bash
setup() {
  load 'test_helper/bats-support/load'
  load 'test_helper/bats-assert/load'
  SCRIPT="${BATS_TEST_DIRNAME}/../scripts/file-guard.sh"
  export CLAUDE_PLUGIN_ROOT="${BATS_TEST_DIRNAME}/.."
}
```

**Hard-block tests (TEST-03 + TEST-08) — Must use `assert_failure 2` (not bare assert_failure):**

Secret/credential files (GRDH-03):
- "exits 2 for .env" — file_path="/project/.env", assert_failure 2
- "exits 2 for .env.local" — file_path="/project/.env.local", assert_failure 2
- "exits 2 for .env.production" — file_path="/project/.env.production", assert_failure 2
- "exits 2 for credentials.json" — file_path="/project/credentials.json", assert_failure 2
- "exits 2 for secret.yaml" — file_path="/project/config/secret.yaml", assert_failure 2
- "exits 2 for server.pem" — file_path="/project/certs/server.pem", assert_failure 2
- "exits 2 for private.key" — file_path="/project/certs/private.key", assert_failure 2

Lock files (GRDH-02):
- "exits 2 for package-lock.json" — assert_failure 2
- "exits 2 for Cargo.lock" — assert_failure 2
- "exits 2 for poetry.lock" — assert_failure 2
- "exits 2 for bun.lock" — assert_failure 2

Generated directories (GRDH-04):
- "exits 2 for node_modules/ path" — file_path="/project/node_modules/pkg/index.js", assert_failure 2
- "exits 2 for .venv/ path" — file_path="/project/.venv/lib/site.py", assert_failure 2
- "exits 2 for target/ path" — file_path="/project/target/debug/main", assert_failure 2

JSON schema verification (TEST-08, PITFALLS.md Pitfall 9):
- "stdout contains permissionDecision deny for .env block" — assert_output --partial '"permissionDecision"' AND assert_output --partial '"deny"'
- "stdout contains valid hookSpecificOutput JSON" — pipe output through jq to verify it parses as JSON and contains hookSpecificOutput.permissionDecision

Block message format (GRDH-08):
- "block message contains AllClear prefix" — run with 2>&1 to capture stderr, assert_output --partial "AllClear"

**Soft-warn tests (TEST-03 soft warns):**

SQL migrations (GRDH-05):
- "exits 0 for SQL migration file" — file_path="/project/migrations/001_init.sql", assert_success
- "outputs warning for SQL migration" — assert_output --partial "AllClear" or assert_output is non-empty (warning present)

Generated code (GRDH-06):
- "exits 0 for .pb.go file" — file_path="/project/api.pb.go", assert_success
- "exits 0 for _generated.ts file" — file_path="/project/types_generated.ts", assert_success
- "exits 0 for .gen.go file" — file_path="/project/models.gen.go", assert_success

CHANGELOG (GRDH-07):
- "exits 0 for CHANGELOG.md" — file_path="/project/CHANGELOG.md", assert_success
- "outputs warning for CHANGELOG.md" — assert_output is non-empty

**Safe file test:**
- "exits 0 for safe file (src/main.py)" — file_path="/project/src/main.py", assert_success
- "produces no output for safe file" — assert_output ""

**Stdin JSON format** (PreToolUse):
```json
{"tool_name":"Write","tool_input":{"file_path":"/project/.env"}}
```

**Critical:** Use `assert_failure 2` (not `assert_failure`) for all hard-block tests per RESEARCH.md Pitfall 3. Exit code 2 is the specific Claude Code exit code for tool denial.
  </action>
  <verify>
    <automated>tests/bats/bin/bats tests/file-guard.bats 2>&1 || echo "Tests failed (expected — scripts/file-guard.sh not yet implemented)"</automated>
  </verify>
  <done>file-guard.bats contains at least 20 @test blocks covering: exit 2 for all hard-blocked file categories (secrets, locks, generated dirs), permissionDecision deny JSON in stdout, AllClear prefix in messages, exit 0 for soft-warn categories (migrations, generated code, CHANGELOG), exit 0 with no output for safe files. All hard-block tests use assert_failure 2 specifically.</done>
</task>

<task type="auto">
  <name>Task 2: Create session-start.bats with context injection tests</name>
  <files>tests/session-start.bats</files>
  <action>
Create tests/session-start.bats covering TEST-04 (session start hook).

**Setup pattern:**
```bash
setup() {
  load 'test_helper/bats-support/load'
  load 'test_helper/bats-assert/load'
  SCRIPT="${BATS_TEST_DIRNAME}/../scripts/session-start.sh"
  FIXTURES_DIR="$(mktemp -d)"
  export CLAUDE_PLUGIN_ROOT="${BATS_TEST_DIRNAME}/.."
}
teardown() {
  rm -rf "${FIXTURES_DIR}"
}
```

**Required test cases:**

Context injection (TEST-04, SSTH-02):
- "outputs additionalContext JSON" — run the session-start script with minimal stdin JSON (SessionStart event has no tool_input — use empty object or `{"event":"SessionStart"}`). assert_output --partial "additionalContext". The session-start hook should output JSON containing an additionalContext field with project type and available commands.
- "additionalContext mentions project type" — create pyproject.toml in FIXTURES_DIR, run script from that directory (cd to FIXTURES_DIR or set appropriate env var), assert_output --partial "python" (or the project type string)

Non-blocking guarantee:
- "exits 0 always" — run with various inputs, assert_success in all cases
- "exits 0 with empty stdin" — pipe empty string, assert_success (graceful handling of missing event data)
- "exits 0 when no manifest files exist" — run from an empty temp dir, assert_success

Disable via env var (SSTH-04):
- "produces no output when ALLCLEAR_DISABLE_SESSION_START=1" — export ALLCLEAR_DISABLE_SESSION_START=1 before running, assert_success, assert_output "" (or verify hook exits early)

Lightweight check (SSTH-03):
- "does not execute any tools" — the hook should only read files, not run formatters/linters. This is hard to test directly, but verify the hook runs quickly (under 2 seconds) by checking that bats test completes fast. Alternatively, verify stdout contains only additionalContext JSON and no tool invocation output.

**Stdin JSON format:** SessionStart events may have a different schema than tool events. Use a minimal JSON blob:
```json
{"event":"SessionStart"}
```
If the script expects no stdin at all for SessionStart, pipe an empty string: `printf '' | bash "${SCRIPT}"`.

**Note:** The exact SessionStart stdin format depends on Phase 6 implementation. Write tests flexibly — assert on output contract (additionalContext JSON present) rather than exact input format. Add a comment noting that the stdin format should be verified against the Phase 6 implementation.
  </action>
  <verify>
    <automated>tests/bats/bin/bats tests/session-start.bats 2>&1 || echo "Tests failed (expected — scripts/session-start.sh not yet implemented)"</automated>
  </verify>
  <done>session-start.bats contains at least 6 @test blocks covering: additionalContext JSON in output, exit 0 in all scenarios, graceful handling of empty stdin, disable via ALLCLEAR_DISABLE_SESSION_START env var, and project type mention in context output. Tests are in RED state until Phase 6 implements session-start.sh.</done>
</task>

</tasks>

<verification>
- `tests/file-guard.bats` has @test blocks for .env, .env.*, credentials, secrets, .pem, .key, all lock file types, node_modules/.venv/target directories
- `tests/file-guard.bats` uses `assert_failure 2` (not bare assert_failure) for all hard-block tests
- `tests/file-guard.bats` verifies permissionDecision deny JSON in stdout
- `tests/file-guard.bats` has soft-warn tests for migrations, generated code, CHANGELOG
- `tests/session-start.bats` verifies additionalContext JSON in output
- `tests/session-start.bats` verifies ALLCLEAR_DISABLE_SESSION_START suppression
- Both files verify exit 0 for non-blocking scenarios
</verification>

<success_criteria>
Guard and session hook test files exist with comprehensive test cases covering TEST-03 (guard blocks and warnings), TEST-04 (session context injection), and TEST-08 (exit 2 with permissionDecision deny JSON). file-guard.bats has at least 20 tests. session-start.bats has at least 6 tests. Tests are in RED state until Phase 5 and Phase 6 implement the scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-tests/13-03-SUMMARY.md`
</output>
