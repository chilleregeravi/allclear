---
phase: 122-verification-gate-release-pin
plan: 02
type: execute
wave: 2
depends_on: ["122-01"]
files_modified:
  - plugins/arcanon/.claude-plugin/plugin.json
  - plugins/arcanon/.claude-plugin/marketplace.json
  - .claude-plugin/marketplace.json
  - plugins/arcanon/package.json
  - plugins/arcanon/package-lock.json
  - plugins/arcanon/CHANGELOG.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md
autonomous: true
requirements: [VER-06, VER-07]
must_haves:
  truths:
    - "All 4 manifest files contain version 0.1.4 (6 version strings total — 1 in plugin.json, 2 in plugins/arcanon/.claude-plugin/marketplace.json, 2 in .claude-plugin/marketplace.json, 1 in package.json)"
    - "package-lock.json is regenerated post-bump (root .version and packages.\"\".version both 0.1.4) so npm ci stays in sync — uses npm install --package-lock-only, NOT npm install"
    - "runtime-deps.json remains absent (Phase 107 deletion preserved)"
    - "CHANGELOG [0.1.4] - <today's UTC date> section is pinned with subsections in Keep-a-Changelog order; fresh empty [Unreleased] heading retained at top; per-bullet REQ-ID references preserved"
    - "ROADMAP.md:241 prose says '8 diagnostic checks' (not '7') — one-line fix reconciling REQUIREMENTS.md:33 canonical truth"
    - "REQUIREMENTS.md:33 prose says '8 smoke-test diagnostics' (not '7') — same one-line fix"
    - "122-VERIFICATION.md exists with status: passed, per-REQ table for VER-01..07, test counts from /tmp/122-*.log, documented non-regressions, v0.1.4 phase summary table (114..122)"
  artifacts:
    - path: ".planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md"
      provides: "Verification gate report — status, per-REQ pass/fail, test counts from Plan 122-01 outputs, v0.1.4 phase summary, verdict"
      min_lines: 100
      contains: "Status: ✅ PASSED"
    - path: "plugins/arcanon/.claude-plugin/plugin.json"
      provides: "Plugin manifest at version 0.1.4"
      contains: '"version": "0.1.4"'
    - path: "plugins/arcanon/.claude-plugin/marketplace.json"
      provides: "Plugin-scoped marketplace at version 0.1.4 (×2 occurrences: plugin entry + top-level)"
      contains: '"version": "0.1.4"'
    - path: ".claude-plugin/marketplace.json"
      provides: "Root marketplace at version 0.1.4 (×2 occurrences: plugin entry + top-level)"
      contains: '"version": "0.1.4"'
    - path: "plugins/arcanon/package.json"
      provides: "npm package at version 0.1.4"
      contains: '"version": "0.1.4"'
    - path: "plugins/arcanon/package-lock.json"
      provides: "Regenerated lockfile so the plugin's own version (0.1.4) appears in both root .version and packages.\"\".version (CI npm ci alignment)"
      contains: '"version": "0.1.4"'
    - path: "plugins/arcanon/CHANGELOG.md"
      provides: "Pinned [0.1.4] - YYYY-MM-DD release section collapsing all 8 phases' [Unreleased] entries; fresh [Unreleased] heading at top"
      contains: "## [0.1.4]"
    - path: ".planning/ROADMAP.md"
      provides: "ROADMAP.md:241 prose fix: '7 diagnostic checks' → '8 diagnostic checks'"
      contains: "8 diagnostic checks"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQUIREMENTS.md:33 prose fix: '7 smoke-test diagnostics' → '8 smoke-test diagnostics'"
      contains: "8 smoke-test diagnostics"
  key_links:
    - from: "plugins/arcanon/CHANGELOG.md [0.1.4] Added subsection"
      to: "Phases 114-121 cumulative additions per RESEARCH §7 collapse map"
      via: "narrative bullets per REQ ID covering NAV/HELP/FRESH/CORRECT/SHADOW/INT capabilities"
      pattern: "/arcanon:list|/arcanon:view|/arcanon:doctor|/arcanon:diff|## Help|/api/scan-freshness|scan_overrides|/arcanon:correct|/arcanon:rescan|/arcanon:shadow-scan|/arcanon:promote-shadow|hub\\.evidence_mode|--offline|--spec|known-externals"
    - from: "plugins/arcanon/CHANGELOG.md [0.1.4] Changed subsection"
      to: "Phase 116 (/arcanon:status freshness extension) + Phase 120 (hub payload schema bump 1.2)"
      via: "narrative bullets"
      pattern: "/arcanon:status|hub payload"
    - from: ".planning/ROADMAP.md and .planning/REQUIREMENTS.md '7 → 8' edits"
      to: "Phase 114-03 SUMMARY (which shipped 8 checks per the canonical numbered list, not 7)"
      via: "one-line text edit reconciling prose to canonical list"
      pattern: "8 (diagnostic checks|smoke-test diagnostics)"
    - from: "122-VERIFICATION.md per-REQ table"
      to: "VER-01..07 in REQUIREMENTS.md + outputs at /tmp/122-*.log"
      via: "row per REQ with pass/fail status, command, expected, actual"
      pattern: "VER-0[1-7]"
---

<objective>
Run the v0.1.4 release **pin half** of the gate after Plan 122-01 verifies clean. Bumps 4 manifest files (6 version strings) from `0.1.3` → `0.1.4` + regenerates `package-lock.json` (VER-06). Pins CHANGELOG `[0.1.4] - <date>` section by collapsing all 8 phases' (114-121) cumulative `[Unreleased]` entries into Keep-a-Changelog-ordered subsections (VER-07). Fixes the ROADMAP.md:241 + REQUIREMENTS.md:33 "7 → 8 diagnostic checks" prose drift identified during Phase 114 plan-phase. Writes `122-VERIFICATION.md` audit-trail report mirroring `113-VERIFICATION.md` exactly.

Purpose: Without this plan, v0.1.4 has no version pin — the manifests still say `0.1.3`, the CHANGELOG has no `[0.1.4]` heading, and the ROADMAP/REQUIREMENTS prose drift survives into the next milestone. This plan is the canonical "ready to tag" gate; after it, `/gsd-complete-milestone v0.1.4` is the next step.

Output: 4 manifest files at `0.1.4` + regenerated `package-lock.json` + pinned CHANGELOG + ROADMAP/REQUIREMENTS one-liner fixes + `122-VERIFICATION.md` recording per-REQ pass/fail with test counts pulled from Plan 122-01's `/tmp/122-*.log` outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-verification-gate-release-pin/122-RESEARCH.md
@.planning/phases/122-verification-gate-release-pin/122-01-PLAN.md
@.planning/milestones/v0.1.3-phases/113-verification-gate/113-01-PLAN.md
@.planning/milestones/v0.1.3-phases/113-verification-gate/113-VERIFICATION.md
@plugins/arcanon/CHANGELOG.md
@plugins/arcanon/.claude-plugin/plugin.json
@plugins/arcanon/.claude-plugin/marketplace.json
@.claude-plugin/marketplace.json
@plugins/arcanon/package.json
@plugins/arcanon/package-lock.json

<interfaces>
<!-- Manifest files and the version strings each holds — VERIFIED 2026-04-25 against current state. -->
<!-- After Phase 107 (v0.1.3), runtime-deps.json is GONE — 4 files / 6 strings is the count. -->

plugins/arcanon/.claude-plugin/plugin.json (1 occurrence at line 3):
```json
{ "name": "arcanon", "version": "0.1.3", ... }
```

plugins/arcanon/.claude-plugin/marketplace.json (2 occurrences at lines 9 + 14):
```json
{
  "name": "arcanon",
  "owner": { "name": "Arcanon" },
  "plugins": [{ "name": "arcanon", "version": "0.1.3", "source": "./plugins/arcanon", "description": "..." }],
  "version": "0.1.3"
}
```

.claude-plugin/marketplace.json (root, 2 occurrences at lines 9 + 14, identical shape):
```json
{
  "name": "arcanon",
  "owner": { "name": "Arcanon" },
  "plugins": [{ "name": "arcanon", "version": "0.1.3", "source": "./plugins/arcanon", "description": "..." }],
  "version": "0.1.3"
}
```

plugins/arcanon/package.json (1 occurrence at line 3):
```json
{ "name": "@arcanon/plugin", "version": "0.1.3", ... }
```

plugins/arcanon/package-lock.json (2 occurrences — regenerated, NOT manually edited):
```json
{ "version": "0.1.3", "packages": { "": { "version": "0.1.3", ... } } }
```

<!-- Lockfile regen incantation (D-02 lesson from v0.1.2 PR #19) -->
```bash
cd /Users/ravichillerega/sources/ligamen/plugins/arcanon
npm install --package-lock-only
```
**NOT** `npm install` — that would install/upgrade dependencies. `--package-lock-only` rewrites the lockfile without touching node_modules. This is what unblocks CI's `npm ci`.

<!-- 122-VERIFICATION.md report shape (mirrors 113-VERIFICATION.md exactly) -->
- Frontmatter: phase, status, verified_at
- "## Status: ✅ PASSED" heading
- Per-REQ section per VER-NN with command + expected + actual (pulled from /tmp/122-*.log)
- v0.1.4 phase summary table (Phase | Status | REQs | Notes) — covers 114, 115, 116, 117, 118, 119, 120, 121, 122
- Breaking changes summary (likely empty for v0.1.4 — additive milestone per RESEARCH §7)
- Verdict line at end: "**v0.1.4 Operator Surface — READY TO SHIP.**"

<!-- CHANGELOG [Unreleased] → [0.1.4] structure (per RESEARCH §7) -->
```
## [Unreleased]
                                ← keep empty after pin (fresh placeholder)

## [0.1.4] - YYYY-MM-DD

### Added
- /arcanon:list (NAV-01) ...
- /arcanon:view (NAV-02) ...
- /arcanon:doctor (NAV-03) ...
- /arcanon:diff (NAV-04) ...
- ## Help sections + --help flag (HELP-01..04) ...
- /api/scan-freshness endpoint (FRESH-03) ...
- /arcanon:status freshness line (FRESH-01, 02, 04) ...
- scan_overrides table + apply hook (CORRECT-01..03) ...
- /arcanon:correct (CORRECT-02, 04..07) ...
- /arcanon:rescan (CORRECT-04, 05) ...
- /arcanon:shadow-scan (SHADOW-01) ...
- /arcanon:diff --shadow (SHADOW-02) ...
- /arcanon:promote-shadow (SHADOW-03) ...
- hub.evidence_mode config (INT-01, 03) ...
- /arcanon:sync --offline (INT-02) ...
- /arcanon:drift openapi --spec (INT-04) ...
- data/known-externals.yaml catalog (INT-05) ...
- External label matching (INT-06..08) ...

### Changed
- /arcanon:status output extension (FRESH-01, 02) ...
- Hub payload schema bumped to 1.2 (INT-01, 03) ...

### Fixed
- (Empty unless a bug was caught and fixed mid-milestone — drop section if empty)

### Removed
- (Empty for v0.1.4 — additive milestone — drop section if empty)

### BREAKING
- (Empty — scan_overrides additive, hub payload 1.2 backward-compatible — drop section if empty)
```

**IMPORTANT — content-derivation rule:** The plan's Task 2 reads the actual `## [Unreleased]` block from CHANGELOG.md (which contains whatever Phases 114-121 cumulatively appended) as the source of truth. The interface above is a TEMPLATE — drop subsections that are empty in the actual `[Unreleased]`. Do NOT ship `### Removed` with no bullets.

<!-- ROADMAP/REQUIREMENTS prose fix (RESEARCH §8) — one-line edits -->
```
ROADMAP.md:241 — Edit:
  Old: "/arcanon:doctor` runs 7 diagnostic checks"
  New: "/arcanon:doctor` runs 8 diagnostic checks"

REQUIREMENTS.md:33 — Edit:
  Old: "**: `/arcanon:doctor` — 7 smoke-test diagnostics:"
  New: "**: `/arcanon:doctor` — 8 smoke-test diagnostics:"
```

The numbered list below each prose line ALREADY contains 8 items (1-8). Only the cardinal number is wrong. Phase 114-03 implementation shipped 8 checks per the list, so the list is canonical and the prose was the drift.
</interfaces>

<background>
Plan 122-02 is the commit-the-version half of Phase 122. It depends on Plan 122-01 having run clean (verify outputs at `/tmp/122-*.log`). It modifies 9 files: 4 manifests + 1 lockfile + CHANGELOG + ROADMAP + REQUIREMENTS + the new 122-VERIFICATION.md report.

Per RESEARCH §9, this two-plan split is intentional — the verify half (122-01) gates the pin half (122-02). If 122-01 fails, do NOT run 122-02; surface the failure and fix the upstream phase first.

Per the orchestrator brief, the ROADMAP/REQUIREMENTS "7 → 8" prose fix is a one-line edit. Don't bloat it into a sub-plan; it's tucked into Task 3 of this plan.
</background>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump 4 manifest files to 0.1.4 + regenerate package-lock.json (VER-06)</name>
  <files>plugins/arcanon/.claude-plugin/plugin.json, plugins/arcanon/.claude-plugin/marketplace.json, .claude-plugin/marketplace.json, plugins/arcanon/package.json, plugins/arcanon/package-lock.json</files>
  <action>
    Per RESEARCH §1 + commit `a9ca133` (v0.1.3 release pin), bump exactly **4 manifest files** (6 version strings total). `runtime-deps.json` is NOT in the list — Phase 107 deleted it (Plan 122-01 Task 1 Grep 3 reconfirmed this).

    1. **`plugins/arcanon/.claude-plugin/plugin.json`** — 1 occurrence at line 3:
       Change `"version": "0.1.3",` → `"version": "0.1.4",`

    2. **`plugins/arcanon/.claude-plugin/marketplace.json`** — 2 occurrences:
       - Line 9 (inside `plugins[0]`): `"version": "0.1.3",` → `"version": "0.1.4",`
       - Line 14 (top-level): `"version": "0.1.3"` → `"version": "0.1.4"`

    3. **`.claude-plugin/marketplace.json`** (root) — 2 occurrences:
       - Line 9 (inside `plugins[0]`): `"version": "0.1.3",` → `"version": "0.1.4",`
       - Line 14 (top-level): `"version": "0.1.3"` → `"version": "0.1.4"`

    4. **`plugins/arcanon/package.json`** — 1 occurrence at line 3:
       Change `"version": "0.1.3",` → `"version": "0.1.4",`

    Use the `Edit` tool for each file. The surrounding context disambiguates the edit (the `"version"` key recurs inside dependency entries, but at line 3 / 9 / 14 the surrounding `"name"` and `"plugins"` context makes each match unique).

    5. **Regenerate `package-lock.json`** (D-02 mandate — same incantation as `a9ca133`):
       ```bash
       cd /Users/ravichillerega/sources/ligamen/plugins/arcanon
       npm install --package-lock-only
       ```
       This rewrites `package-lock.json` so the root `.version` and `packages.""."version"` both read `0.1.4` (matching the bumped `package.json`). It does **not** install dependencies; it only re-derives the lockfile. This is what unblocks CI's `npm ci`.

       **DO NOT use** `npm install` (no flag) — that would install/upgrade dependencies and create unrelated diff churn.

    Verify post-bump:
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    grep -nH '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json plugins/arcanon/package-lock.json | grep -v "node_modules"
    ```
    Expected: every match shows `"0.1.4"`. Total count: 6 strings in the 4 manifest files + 2 in package-lock.json (root .version + packages.""."version") = 8 total `"0.1.4"` strings across 5 files. Manifest count from VER-06's perspective is 4 (lockfile is generated, not authored).

    Confirm `runtime-deps.json` is still gone (sanity, in case anything regenerated it):
    ```bash
    test ! -f plugins/arcanon/runtime-deps.json && echo "OK: runtime-deps.json absent"
    ```
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && [ "$(grep -h '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json | grep -c '"0.1.4"')" -eq 6 ] && grep -q '"version": "0.1.4"' plugins/arcanon/package-lock.json && test ! -f plugins/arcanon/runtime-deps.json</automated>
  </verify>
  <done>All 6 manifest version strings across 4 files read `0.1.4`. package-lock.json regenerated via `npm install --package-lock-only` and contains `0.1.4` in both root and packages."" entries (8 total `"0.1.4"` strings across 5 files). runtime-deps.json absence reconfirmed. `npm ci` would now pass (verifiable post-commit on CI).</done>
</task>

<task type="auto">
  <name>Task 2: Pin CHANGELOG [0.1.4] section (VER-07)</name>
  <files>plugins/arcanon/CHANGELOG.md</files>
  <action>
    Per RESEARCH §7 + commit `47648fb` (v0.1.3 CHANGELOG pin), capture today's UTC date once and reuse it:
    ```bash
    TODAY=$(date -u +%Y-%m-%d)
    echo "$TODAY"
    ```
    Use the value (e.g., `2026-04-XX`) literally in the CHANGELOG section heading.

    **Step 1: Read the current `## [Unreleased]` block.** This is the source of truth — Phases 114-121 have each appended bullets to it during their execution. The block currently lives between `## [Unreleased]` (line 7) and `## [0.1.3] - 2026-04-25` (line 31, current state).

    Inspect the contents:
    ```bash
    awk '/^## \[Unreleased\]/,/^## \[0\.1\.3\]/' plugins/arcanon/CHANGELOG.md
    ```

    **Step 2: Re-organize the [Unreleased] block into a new `## [0.1.4] - $TODAY` section.** Keep-a-Changelog subsection order: `### BREAKING` → `### Added` → `### Changed` → `### Deprecated` → `### Removed` → `### Fixed` → `### Security`. Drop empty subsections (do NOT ship `### Removed` with no bullets).

    The expected v0.1.4 collapse per RESEARCH §7 (template — actual narrative re-derived from each phase's actual SUMMARY/Unreleased entries):

    ```markdown
    ## [Unreleased]

    ## [0.1.4] - YYYY-MM-DD

    ### Added

    - **`/arcanon:list` command** (NAV-01). Concise project overview — linked
      repos, services partitioned by type, connection counts by confidence,
      external actor count, and hub sync status. Read-only via worker HTTP.
      Silent in non-Arcanon directories. Supports `--json` for machine
      consumption.
    - **`/arcanon:view` command** (NAV-02). Top-level slash-command alias for
      `/arcanon:map view` — opens the graph UI in your default browser. Pure
      markdown command (no Node-side handler); existing `/arcanon:map view`
      keystroke preserved for back-compat.
    - **`/arcanon:doctor` command** (NAV-03). 8 smoke-test diagnostics with
      PASS/WARN/FAIL/SKIP per check and structured exit codes (0=all-pass or
      non-critical WARN; 1=critical fail). Critical: worker reachable, data
      dir writable, DB integrity. Non-critical: version match, schema head,
      config + linked repos, MCP smoke, hub credentials. Migration head
      computed dynamically from filesystem glob. Supports `--json`. Silent
      in non-Arcanon directories.
    - **`/arcanon:diff <scanA> <scanB>` command** (NAV-04). Compare two
      scan_versions. Accepts integer IDs, `HEAD` / `HEAD~N`, ISO timestamps,
      or branch heuristics. Output groups changes by services
      added/removed/modified and connections added/removed/modified.
    - **`## Help` sections + `--help` flag for every command** (HELP-01..04).
      Every `/arcanon:*` command supports `--help` / `-h` / `help`. Output
      is the `## Help` section content from the command's markdown file
      (single source of truth — no separate help text file). bats test
      iterates every command + asserts non-empty output and exit 0.
    - **`/api/scan-freshness` worker endpoint** (FRESH-03). Returns
      `{last_scan_iso, last_scan_age_seconds, scan_quality_pct, repos:
      [{name, path, last_scanned_sha, new_commits}]}`. Mirrors
      `/api/scan-quality` and `/api/version` patterns.
    - **`/arcanon:status` freshness line** (FRESH-01, 02, 04). New per-repo
      signal computed from `git log <last_scan_sha>..HEAD --oneline | wc -l`
      per tracked repo. Reports count of repos with un-scanned commits since
      last scan, and the number of new commits in each.
    - **`scan_overrides` table + apply hook** (CORRECT-01..03). Migration
      017 adds the table (kind ∈ {connection, service}, action ∈ {delete,
      update, rename, set-base-path}, payload JSON, applied_in_scan_version_id
      nullable). Scan pipeline reads pending overrides BEFORE `endScan` and
      applies them to persisted findings; idempotent re-apply.
    - **`/arcanon:correct` command** (CORRECT-02, 04..07). Four actions:
      delete, update (re-point connection), rename (service rename across
      all references), set-base-path. Supports `connection <id>` and
      `service <name>` targets.
    - **`/arcanon:rescan <repo-path-or-name>` command** (CORRECT-04, 05).
      Re-scans exactly one repo. Bypasses incremental change-detection skip.
      Updates `scan_versions` for that repo only. Accepts filesystem path or
      `repos.name` registered identifier. Non-existent repo exits 2 with
      friendly error.
    - **`/arcanon:shadow-scan` command** (SHADOW-01). Writes to
      `$ARCANON_DATA_DIR/projects/<hash>/impact-map-shadow.db` instead of
      `impact-map.db`. Same scan code path; different DB target via env
      var or scan flag.
    - **`/arcanon:diff --shadow` mode** (SHADOW-02). Compares shadow DB vs
      live for the current project. Reuses the Phase 115 diff engine.
    - **`/arcanon:promote-shadow` command** (SHADOW-03). Atomically: (1)
      backs up `impact-map.db` to `impact-map.db.pre-promote-<timestamp>`,
      (2) renames `impact-map-shadow.db` → `impact-map.db`, (3) reports
      backup path on stdout.
    - **`hub.evidence_mode` config flag** (INT-01, 03). Values: `"full"`
      (default — back-compat), `"hash-only"` (replaces evidence string with
      `{hash, start_line, end_line}`), `"none"`. Hub Payload schema bumped
      to 1.2 (back-compat: 1.0/1.1 receivers see byte-identical evidence
      shape for `"full"` default).
    - **`/arcanon:sync --offline` flag** (INT-02). Exits 0 with "scan
      persisted locally, no upload" message. Differentiates intentional
      offline (clean exit) from hub-unreachable (transient — different
      message).
    - **`/arcanon:drift openapi --spec <path>` flag** (INT-04). Bypasses
      `discoverOpenApiSpecs()` discovery. Repeatable: `--spec a.yaml --spec
      b.yaml` compares two explicit specs.
    - **`data/known-externals.yaml` catalog** (INT-05). Ships ~20 common
      third parties (Stripe, Auth0, OpenTelemetry, S3/Azure/GCS, GitHub,
      Slack, PagerDuty, Sentry, Datadog, Twilio, SendGrid, plus 8-10 more).
    - **External label matching** (INT-06..08). Scan enrichment loads
      `known-externals.yaml` and matches actor URLs/host patterns against
      the catalog. Matched actors get a friendly `label` field. User
      extension via `arcanon.config.json` `external_labels` (user takes
      precedence on key collision). `/arcanon:list` and graph UI display
      labeled name instead of raw URL when present.

    ### Changed

    - **`/arcanon:status` output extension** (FRESH-01, 02). New
      "Latest scan: <date> (NN% high-conf)" line + "N repos have new commits
      since last scan: api (12 new), worker (3 new)" line.
    - **Hub payload schema bumped to 1.2** (INT-01, 03). Back-compat: v1.0/1.1
      receivers see byte-identical `evidence` field for `evidence_mode: "full"`
      (default). Only the version bumps; `evidence` shape change is opt-in via
      `evidence_mode: "hash-only"`.
    ```

    **Step 3: Apply the actual content from `## [Unreleased]`.** The template above is a SEED — the actual collapse must use the actual bullets that Phases 114-121 appended. If a phase forgot to append (the `## [Unreleased]` block is missing an entry the template predicts), surface to user — that's a phase-execution gap, not a 122-02 fix.

    **Step 4: Drop empty subsections.** The template includes `### Fixed`, `### Removed`, `### BREAKING` only IF the actual `[Unreleased]` block has bullets in those subsections. Per RESEARCH §7, v0.1.4 is an additive milestone — likely no `### BREAKING`, no `### Removed`, possibly empty `### Fixed`. Drop them rather than ship empty headings.

    **Step 5: Verify the structure post-edit:**
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    grep -n "^## " plugins/arcanon/CHANGELOG.md | head -5
    ```
    Expected first 3 lines:
    ```
    7:## [Unreleased]
    9:## [0.1.4] - 2026-04-XX       ← today's UTC date
    NN:## [0.1.3] - 2026-04-25      ← unchanged
    ```

    And confirm the subsections under `## [0.1.4]` are in Keep-a-Changelog order:
    ```bash
    awk '/^## \[0\.1\.4\]/,/^## \[0\.1\.3\]/' plugins/arcanon/CHANGELOG.md | grep -E "^### "
    ```
    Expected output (only the non-empty subsections, in this order):
    ```
    ### Added       ← always present
    ### Changed     ← present (FRESH + INT changes)
    ### Fixed       ← only if non-empty
    ### Removed     ← only if non-empty
    ### BREAKING    ← only if non-empty (likely absent for v0.1.4)
    ```

    All non-empty subsections present, in Keep-a-Changelog order. Per-bullet REQ-ID references preserved (e.g., `(NAV-01)`, `(HELP-01..04)`).
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && grep -q "^## \[0.1.4\] - $(date -u +%Y-%m-%d)$" plugins/arcanon/CHANGELOG.md && [ "$(awk '/^## \[0\.1\.4\]/,/^## \[0\.1\.3\]/' plugins/arcanon/CHANGELOG.md | grep -cE '^### (Added|Changed|Fixed|Removed|BREAKING|Deprecated|Security)$')" -ge 1 ] && grep -q "^## \[Unreleased\]$" plugins/arcanon/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG `[0.1.4] - YYYY-MM-DD` section exists with today's UTC date, contains at least `### Added` (mandatory — v0.1.4 is additive), Keep-a-Changelog subsection order respected, per-bullet REQ-ID references preserved, fresh empty `[Unreleased]` heading sits above it. Empty subsections dropped (no `### Removed` with zero bullets).</done>
</task>

<task type="auto">
  <name>Task 3: Fix ROADMAP + REQUIREMENTS "7 → 8 doctor checks" prose drift</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    Per RESEARCH §8, both ROADMAP.md:241 and REQUIREMENTS.md:33 say "7" diagnostic checks but list 8 numbered items below. Phase 114-03 shipped 8 checks per the canonical list. The prose number is the drift; fix it.

    **Edit 1 — `.planning/ROADMAP.md`** at line 241:
    - Old: `\`/arcanon:doctor\` runs 7 diagnostic checks`
    - New: `\`/arcanon:doctor\` runs 8 diagnostic checks`

    Use the `Edit` tool with sufficient surrounding context to make the match unique. The line is inside the Phase 114 details paragraph.

    **Edit 2 — `.planning/REQUIREMENTS.md`** at line 33:
    - Old: `**: \`/arcanon:doctor\` — 7 smoke-test diagnostics:`
    - New: `**: \`/arcanon:doctor\` — 8 smoke-test diagnostics:`

    Use the `Edit` tool. The line is the NAV-03 requirement heading; the numbered list (1-8) below is unchanged.

    **Verify both edits:**
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    grep -n "diagnostic checks\|smoke-test diagnostics" .planning/ROADMAP.md .planning/REQUIREMENTS.md
    ```
    Expected: every match reads "**8** diagnostic checks" / "**8** smoke-test diagnostics", zero "7" cardinals on those lines.

    These edits are atomic (one number changes per file) and have zero blast radius — they're prose-only documentation fixes.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && grep -q "8 diagnostic checks" .planning/ROADMAP.md && grep -q "8 smoke-test diagnostics" .planning/REQUIREMENTS.md && ! grep -q "7 diagnostic checks" .planning/ROADMAP.md && ! grep -q "7 smoke-test diagnostics" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ROADMAP.md says "8 diagnostic checks" (zero "7 diagnostic checks" hits). REQUIREMENTS.md says "8 smoke-test diagnostics" (zero "7 smoke-test diagnostics" hits). Prose now matches the canonical 8-item numbered list and the Phase 114-03 implementation.</done>
</task>

<task type="auto">
  <name>Task 4: Write 122-VERIFICATION.md report</name>
  <files>.planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md</files>
  <action>
    Mirror the structure of `.planning/milestones/v0.1.3-phases/113-verification-gate/113-VERIFICATION.md` exactly. Use the captured outputs from Plan 122-01's Tasks 1-5 (`/tmp/122-grep-output.log`, `/tmp/122-bats-output.log`, `/tmp/122-node-output.log`, `/tmp/122-help-output.log`, `/tmp/122-doctor.log`) plus the manifest/CHANGELOG state from this plan's Tasks 1-2.

    File path: `.planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md`

    **Structure (template — substitute placeholders with actual captured values):**

    ```markdown
    ---
    phase: 122-verification-gate-release-pin
    status: passed
    verified_at: <today's UTC date, same as CHANGELOG>
    ---

    # Phase 122: Verification Gate + Release Pin

    ## Status: ✅ PASSED

    Milestone v0.1.4 release gate verified. Read-only navigability commands
    (Phase 114-115), `--help` system + `/arcanon:status` freshness extension
    (Phase 116), `scan_overrides` write-side infrastructure + operator commands
    (Phases 117-118), shadow scan workflow (Phase 119), and integration data +
    consumption layers (Phases 120-121) all verify clean. Manifests pinned at
    0.1.4 across 4 files (6 version strings); `package-lock.json` regenerated;
    CHANGELOG `[0.1.4]` section pinned with all non-empty subsections in
    Keep-a-Changelog order; ROADMAP/REQUIREMENTS prose drift on `/arcanon:doctor`
    check count (7 → 8) reconciled.

    ## Per-REQ Status

    | REQ    | Description                                                                                                          | Status | Evidence |
    | ------ | -------------------------------------------------------------------------------------------------------------------- | ------ | -------- |
    | VER-01 | bats green ≥340 baseline + new tests                                                                                 | ✅     | <count>/<total> passing; HOK-06 macOS caveat <hit/not hit> at threshold=200; output at /tmp/122-bats-output.log |
    | VER-02 | node green for affected modules (117-121)                                                                            | ✅     | <count>/<total> passing; only documented v0.1.3 pre-existing manager.test.js incremental-prompt failure persists |
    | VER-03 | All commands have `## Help` + `--help` non-empty + exit 0                                                            | ✅     | <NN>/<NN> commands PASS (Plan 122-01 Task 4); output at /tmp/122-help-output.log |
    | VER-04 | Refined `--help` grep + carry-over absence checks                                                                    | ✅     | 4 grep families clean; output at /tmp/122-grep-output.log |
    | VER-05 | Fresh-install Node 25 smoke                                                                                          | <✅ Pattern A / ⚠️ Pattern B deferred> | <Pattern A: zero critical FAIL in /arcanon:doctor / Pattern B: deferred-with-justification per 105/113 precedent>; output at /tmp/122-doctor.log |
    | VER-06 | 4 manifests at 0.1.4 (6 strings) + lockfile regen                                                                    | ✅     | 6 `"0.1.4"` matches across 4 manifest files; package-lock.json regenerated via `npm install --package-lock-only` |
    | VER-07 | CHANGELOG [0.1.4] pinned with subsections in Keep-a-Changelog order                                                  | ✅     | `## [0.1.4] - <date>` heading present; subsections in order; fresh empty `[Unreleased]` heading at top |

    ## VER-01 — bats Suite

    **Command:**
    ```bash
    IMPACT_HOOK_LATENCY_THRESHOLD=200 bats tests/
    ```

    **Result:** <count>/<total> passing.

    Pull the actual output excerpt from `/tmp/122-bats-output.log` (last 5-10 lines, the summary block).

    **Phase 114-121 added test files (all green):**
    - `tests/list.bats` (Phase 114, NAV-01)
    - `tests/doctor.bats` (Phase 114, NAV-03)
    - `tests/diff.bats` (Phase 115, NAV-04)
    - `tests/help.bats` (Phase 116, HELP-04)
    - `tests/scan-freshness.bats` (Phase 116, FRESH-05)
    - `tests/correct.bats` (Phase 118, CORRECT-07)
    - `tests/rescan.bats` (Phase 118, CORRECT-07)
    - `tests/shadow-scan.bats` (Phase 119, SHADOW-04)
    - `tests/drift-openapi-spec.bats` (Phase 120, INT-10)

    **HOK-06 macOS caveat:** <Document if hit, mirroring 113-VERIFICATION.md:63-70 wording. If not hit, omit this section.>

    ## VER-02 — node Suite

    **Command:**
    ```bash
    cd plugins/arcanon && npm test
    ```

    **Result:** <count>/<total> passing across <NN> test suites.

    Pull the actual output excerpt from `/tmp/122-node-output.log` (the `ℹ tests N / pass M / fail K / duration_ms T` block).

    **Pre-existing non-regression failure** (carried from v0.1.3, unrelated to v0.1.4):
    - `worker/scan/manager.test.js:676` — `incremental scan prompt contains INCREMENTAL_CONSTRAINT heading and changed filename` (`TypeError: Cannot read properties of undefined (reading 'prepare')` at `worker/scan/manager.js:806` — mock fixture missing `_db`).

    **v0.1.4-added tests (all green):**
    - Phase 117: migration 017 idempotency, query-engine scan_overrides reads, scan apply-overrides flow
    - Phase 118: cmdCorrect / cmdRescan units
    - Phase 119: pool.js shadow cache-key, scan target via env/flag
    - Phase 120: hub-sync payload 1.2, sync.js --offline path, drift openapi --spec, known-externals loader
    - Phase 121: enrichment catalog match, query-engine label join, external_labels merge

    ## VER-03 — Per-Command `--help` Smoke

    **Command:** Plan 122-01 Task 4 iterated `plugins/arcanon/commands/*.md` and invoked `bash hub.sh <name> --help` for each.

    **Result:** <NN>/<NN> commands PASS (zero FAILs). Pull excerpt from `/tmp/122-help-output.log`.

    Commands tested (sorted alphabetically):
    - correct, diff, doctor, drift, export, impact, list, login, map, promote-shadow, rescan, shadow-scan, status, sync, update, verify, view (~17)

    Each returned non-empty stdout (`## Help` section content extracted via Phase 116's `awk` detector) and exit 0.

    ## VER-04 — Refined `--help` Grep + Absence Carry-Overs

    | Check                                                                          | Result |
    | ------------------------------------------------------------------------------ | ------ |
    | `--help` outside `## Help` blocks in `commands/*.md` (excluding HELP-03 ref)   | ✅ zero leaks |
    | `--help` in README/skills/hooks (out-of-scope for v0.1.4 HELP system)          | ✅ zero matches |
    | `runtime-deps.json` absent + grep clean (v0.1.3 carry-over)                    | ✅ file absent + grep clean |
    | `commands/upload.md` absent + grep clean in README/skills (v0.1.3 carry-over)  | ✅ file absent + grep clean |

    The refined `--help` grep (RESEARCH §5) allows: (a) `## Help` section content, (b) `commands/update.md:21` `claude plugin update --help` host-CLI reference (HELP-03 explicit exception), (c) `lib/help.sh` detector implementation. Everything else is denied. Plan 122-01 Task 1 confirmed zero leaks.

    ## VER-05 — Fresh-install Smoke (Node 25)

    <Pick one of the two patterns based on what Plan 122-01 Task 5 actually ran:>

    **Pattern A — performed in-session:**
    ```bash
    nvm use 25  # Node v25.x.x
    git clone https://github.com/Arcanon-hub/arcanon /tmp/arcanon-fresh-<ts>
    cd /tmp/arcanon-fresh-<ts>/plugins/arcanon && npm install
    bash scripts/install-deps.sh   # no-op happy path
    bash scripts/session-start.sh
    ARCANON_DATA_DIR=/tmp/arcanon-fresh-<ts>-data bash scripts/hub.sh doctor
    ```
    Result: <PASS — zero critical FAIL in /arcanon:doctor (checks 1, 5, 6) — pull from /tmp/122-doctor.log>

    **Pattern B — deferred to pre-tag manual run** (mirrors 113-VERIFICATION.md:26):
    The fresh-install machinery is structurally unchanged from v0.1.3 (`scripts/install-deps.sh`, `scripts/mcp-wrapper.sh` both stable since Phase 107). Phase 107's INST-07..11 bats fixtures cover the install-deps.sh contract (`tests/install-deps.bats`, 11 tests, all green per VER-01). Manual smoke deferred to pre-tag run; recorded here once executed.

    Status: ⚠️ deferred (acceptable per 105/113 precedent)

    ## VER-06 — Manifest Bump

    | File                                              | Occurrences | Status |
    | ------------------------------------------------- | ----------- | ------ |
    | `plugins/arcanon/.claude-plugin/plugin.json`      | 1           | ✅ "0.1.4" |
    | `plugins/arcanon/.claude-plugin/marketplace.json` | 2           | ✅ both "0.1.4" |
    | `.claude-plugin/marketplace.json`                 | 2           | ✅ both "0.1.4" |
    | `plugins/arcanon/package.json`                    | 1           | ✅ "0.1.4" |
    | **Total**                                         | **6 strings / 4 files** | ✅ |
    | `plugins/arcanon/package-lock.json`               | 2 (regenerated via `npm install --package-lock-only`) | ✅ "0.1.4" |

    `runtime-deps.json` is intentionally **not** in this list — Phase 107's INST-01 deleted it. Manifest count for v0.1.4 is 4, identical to v0.1.3 (no new manifests introduced).

    ## VER-07 — CHANGELOG Pin

    `## [0.1.4] - <today's UTC date>` heading present. Subsections in Keep-a-Changelog order:

    | Subsection    | Required Coverage                                                                                                               | Status |
    | ------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------ |
    | ### Added     | NAV-01..04 (list/view/doctor/diff), HELP-01..04 (--help), FRESH-03 (endpoint), CORRECT-01..07 (scan_overrides + correct/rescan), SHADOW-01..03 (shadow-scan/diff/promote), INT-01..08 (evidence_mode, --offline, --spec, externals catalog + matcher) | ✅ |
    | ### Changed   | FRESH-01, 02 (/arcanon:status output extension), INT-01, 03 (hub payload schema bump 1.2)                                       | ✅ |
    | ### Fixed     | (only if non-empty in [Unreleased])                                                                                             | <✅ or omitted> |
    | ### Removed   | (none planned for v0.1.4)                                                                                                       | omitted |
    | ### BREAKING  | (none — scan_overrides additive, hub payload backward-compat)                                                                   | omitted |

    Fresh empty `## [Unreleased]` heading retained at top for the next milestone cycle.

    ## ROADMAP/REQUIREMENTS Prose Drift Reconciliation

    Per RESEARCH §8 + Phase 114 plan-phase finding, the following one-line edits land in this plan:

    | File                                | Old                                          | New                                          |
    | ----------------------------------- | -------------------------------------------- | -------------------------------------------- |
    | `.planning/ROADMAP.md:241`          | `runs 7 diagnostic checks`                   | `runs 8 diagnostic checks`                   |
    | `.planning/REQUIREMENTS.md:33`      | `— 7 smoke-test diagnostics:`                | `— 8 smoke-test diagnostics:`                |

    Both files' prose now matches the canonical 8-item numbered list and the Phase 114-03 implementation (which shipped 8 checks).

    ## Summary of Phases (v0.1.4)

    | Phase | Status | REQs | Notes |
    |---|---|---|---|
    | 114 Read-Only Navigability Commands (`/list`, `/view`, `/doctor`) | ✅ | 3/3 | NAV-01 cmdList composition; NAV-02 pure-markdown alias; NAV-03 8 diagnostics with --json |
    | 115 Scan-Version Diff Command (`/diff`) | ✅ | 1/1 | NAV-04 — 4 input forms (ID, HEAD~N, ISO, branch heuristic) |
    | 116 `--help` System + `/arcanon:status` Freshness | ✅ | 9/9 | HELP-01..04 + FRESH-01..05 — bash detector + awk extraction + new endpoint |
    | 117 scan_overrides Persistence Layer | ✅ | 3/3 | CORRECT-01..03 — Migration 017 + apply hook between persistFindings and endScan |
    | 118 scan_overrides Operator Commands | ✅ | 4/4 | CORRECT-04..07 — `/correct` 4 actions + `/rescan` single-repo |
    | 119 Shadow Scan + Atomic Promote | ✅ | 4/4 | SHADOW-01..04 — sibling DB, diff --shadow, atomic promote with backup |
    | 120 Integration Data Layer | ✅ | 5/5 | INT-01..05 — evidence_mode + --offline + --spec + known-externals shipped |
    | 121 Integration Consumption Layer | ✅ | 5/5 | INT-06..10 — catalog matcher + user external_labels + UI surfacing |
    | 122 Verification Gate + Release Pin | ✅ | 7/7 | This report |
    | **Total** | **✅** | **41/41** | |

    ## Breaking Changes Summary (for release notes)

    None. v0.1.4 is an **additive milestone**:
    - `scan_overrides` table is new (migration 017) — additive, no existing column changes.
    - Hub payload schema bumped from 1.0/1.1 → 1.2 — backward-compatible (default `evidence_mode: "full"` produces byte-identical `evidence` field shape; only the version string in the payload header changes).
    - All new commands (`list`, `view`, `doctor`, `diff`, `correct`, `rescan`, `shadow-scan`, `promote-shadow`) are additive — none replace or remove existing commands.
    - All command markdown files gain a `## Help` section (HELP-01) — additive content; existing run sections unchanged.

    ## Verdict

    **v0.1.4 Operator Surface — READY TO SHIP.**

    All 41 requirements complete. Test suites green (with 1 documented macOS HOK-06 latency caveat at threshold=200 if hit, and 1 pre-existing node failure carried from v0.1.3 — `manager.test.js:676` incremental-prompt mock — unrelated to v0.1.4). Manifests pinned at 0.1.4. CHANGELOG complete. ROADMAP/REQUIREMENTS prose drift reconciled. Next step: `/gsd-complete-milestone v0.1.4`.
    ```

    **Substitute placeholders:**
    - `<count>/<total>` ← actual numbers from /tmp/122-bats-output.log + /tmp/122-node-output.log + /tmp/122-help-output.log
    - `<today's UTC date>` ← `$(date -u +%Y-%m-%d)` (same as CHANGELOG `[0.1.4]` heading)
    - `<NN>` ← actual command count from /tmp/122-help-output.log
    - Pattern A vs Pattern B section ← whichever Plan 122-01 Task 5 ran
    - HOK-06 caveat section ← include with 113-VERIFICATION.md wording if hit, omit if not
    - Phase summary REQ counts ← actual REQ counts from each phase's REQUIREMENTS.md mapping

    Verify the report:
    ```bash
    cd /Users/ravichillerega/sources/ligamen
    test -f .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md
    grep -q "Status: ✅ PASSED" .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md
    [ "$(grep -cE 'VER-0[1-7]' .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md)" -ge 7 ]
    grep -q "READY TO SHIP" .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md
    ```
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && test -f .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md && [ "$(grep -cE 'VER-0[1-7]' .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md)" -ge 7 ] && grep -q "Status: ✅ PASSED" .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md && grep -q "READY TO SHIP" .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md</automated>
  </verify>
  <done>122-VERIFICATION.md exists at the canonical path, has frontmatter with `status: passed`, contains a per-REQ table covering VER-01 through VER-07 (each marked ✅ or ⚠️ deferred for VER-05 Pattern B), includes the v0.1.4 phase summary table (114..122 with REQ counts totaling 41/41), the breaking-changes summary (none — additive milestone), the ROADMAP/REQUIREMENTS prose-drift reconciliation table, and a final "READY TO SHIP" verdict line. The report is the audit trail for `/gsd-complete-milestone v0.1.4`.</done>
</task>

</tasks>

<threat_model>

`security_enforcement` is not enabled for this phase. Plan 122-02 edits version strings in 4 JSON manifests, regenerates a lockfile via `npm install --package-lock-only`, edits a CHANGELOG markdown file, edits two prose-only one-line drifts in planning markdown, and writes a verification report. No new trust boundaries, no new untrusted-input ingestion, no new code-execution surfaces. STRIDE register is not applicable.

</threat_model>

<verification>
Plan 122-02 passes when all 4 tasks complete and:

1. **Manifests bumped (Task 1):** 4 files / 6 strings at `0.1.4`. `package-lock.json` regenerated via `npm install --package-lock-only` (root `.version` and `packages.""."version"` both 0.1.4). `runtime-deps.json` absence confirmed.

2. **CHANGELOG pinned (Task 2):** `## [0.1.4] - YYYY-MM-DD` section with subsections in Keep-a-Changelog order (`### Added` mandatory; `### Changed` likely; others only if non-empty in source `[Unreleased]`). Per-bullet REQ-ID references preserved. Fresh empty `## [Unreleased]` heading at top.

3. **ROADMAP/REQUIREMENTS prose fixed (Task 3):** Both files say "8 diagnostic checks" / "8 smoke-test diagnostics"; zero "7" cardinals on those lines. The prose now matches the canonical 8-item numbered list and Phase 114-03's implementation.

4. **Verification report exists (Task 4):** `122-VERIFICATION.md` mirrors `113-VERIFICATION.md` shape with per-REQ table (VER-01..07), test counts pulled from `/tmp/122-*.log`, v0.1.4 phase summary (114..122, 41/41 REQs), breaking-changes summary (none — additive), ROADMAP/REQUIREMENTS prose-drift reconciliation table, "READY TO SHIP" verdict.

Single end-to-end check command (run from repo root):
```bash
cd /Users/ravichillerega/sources/ligamen
[ "$(grep -h '"version"' .claude-plugin/marketplace.json plugins/arcanon/.claude-plugin/plugin.json plugins/arcanon/.claude-plugin/marketplace.json plugins/arcanon/package.json | grep -c '"0.1.4"')" -eq 6 ] \
  && grep -q '"version": "0.1.4"' plugins/arcanon/package-lock.json \
  && test ! -f plugins/arcanon/runtime-deps.json \
  && grep -q "^## \[0.1.4\] - $(date -u +%Y-%m-%d)$" plugins/arcanon/CHANGELOG.md \
  && grep -q "^## \[Unreleased\]$" plugins/arcanon/CHANGELOG.md \
  && grep -q "8 diagnostic checks" .planning/ROADMAP.md \
  && grep -q "8 smoke-test diagnostics" .planning/REQUIREMENTS.md \
  && ! grep -q "7 diagnostic checks" .planning/ROADMAP.md \
  && ! grep -q "7 smoke-test diagnostics" .planning/REQUIREMENTS.md \
  && test -f .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md \
  && grep -q "READY TO SHIP" .planning/phases/122-verification-gate-release-pin/122-VERIFICATION.md \
  && echo "Plan 122-02 pin: PASS"
```
</verification>

<success_criteria>
- [ ] VER-06: 4 manifests / 6 version strings at `0.1.4`; package-lock.json regenerated via `npm install --package-lock-only`; runtime-deps.json absence confirmed
- [ ] VER-07: CHANGELOG `[0.1.4] - YYYY-MM-DD` section pinned; subsections in Keep-a-Changelog order; per-bullet REQ-ID refs preserved; fresh `[Unreleased]` heading at top
- [ ] ROADMAP.md:241 prose says "8 diagnostic checks" (was "7"); REQUIREMENTS.md:33 prose says "8 smoke-test diagnostics" (was "7")
- [ ] `122-VERIFICATION.md` written with per-REQ table (VER-01..07), test counts from `/tmp/122-*.log`, v0.1.4 phase summary (114..122, 41/41), breaking-changes summary (none — additive), prose-drift reconciliation table, "READY TO SHIP" verdict
- [ ] All edits committed via git with `chore(release): ...` (manifest bump + CHANGELOG pin) and `docs: ...` (ROADMAP/REQUIREMENTS prose fix) commit prefixes — orchestrator handles commits, this plan does not
</success_criteria>

<output>
After completion, create `.planning/phases/122-verification-gate-release-pin/122-02-SUMMARY.md` covering:
- Manifest bump diff (which files changed, which strings)
- CHANGELOG pinned date + subsection list (which were present, which were omitted as empty)
- ROADMAP.md / REQUIREMENTS.md one-line edits (line numbers + before/after)
- Path to `122-VERIFICATION.md`
- Verdict: ready for `/gsd-complete-milestone v0.1.4`
</output>
