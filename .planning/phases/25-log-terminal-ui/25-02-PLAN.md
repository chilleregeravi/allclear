---
phase: 25-log-terminal-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - worker/ui/graph.js
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04

must_haves:
  truths:
    - "The log panel is visible and collapsed when the graph UI first loads"
    - "Clicking the panel header opens it and new log lines appear within 2 seconds"
    - "The component dropdown filters the visible lines to one subsystem"
    - "The search input filters visible lines to those matching the typed string"
    - "The panel auto-scrolls to the newest line when at the bottom; stops when scrolled up"
  artifacts:
    - path: "worker/ui/graph.js"
      provides: "Wires initLogTerminal() call after project loads"
      contains: "initLogTerminal"
  key_links:
    - from: "worker/ui/graph.js"
      to: "worker/ui/modules/log-terminal.js"
      via: "import { initLogTerminal } and call after graph data loads"
      pattern: "initLogTerminal\\(\\)"
---

<objective>
Wire `initLogTerminal()` into `graph.js` so the log panel activates after a project loads.
Then verify the full end-to-end behavior in the browser: panel toggle, live polling, component
filter, keyword search, and auto-scroll.

Purpose: Connects the log terminal module to the application lifecycle.
Output: Fully working log terminal panel in the graph UI.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-log-terminal-ui/25-01-SUMMARY.md
@worker/ui/graph.js
@worker/ui/modules/log-terminal.js

<interfaces>
<!-- Extracted from graph.js — the only change needed is two lines -->

Current graph.js imports:
```javascript
import { state } from "./modules/state.js";
import { render } from "./modules/renderer.js";
import { setupInteractions, setupControls } from "./modules/interactions.js";
import { hideDetailPanel } from "./modules/detail-panel.js";
import { showProjectPicker } from "./modules/project-picker.js";
```

Add one import line:
```javascript
import { initLogTerminal } from "./modules/log-terminal.js";
```

In the `init()` function, after `setupControls()` and the `detail-close` listener, add:
```javascript
initLogTerminal();
```

That is the complete change to graph.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire initLogTerminal into graph.js</name>
  <files>worker/ui/graph.js</files>
  <action>
    Make exactly two changes to graph.js:

    1. Add import at the top with other imports:
       ```javascript
       import { initLogTerminal } from "./modules/log-terminal.js";
       ```

    2. Inside `init()`, after the `setupControls()` call and after the `detail-close` addEventListener block, add:
       ```javascript
       initLogTerminal();
       ```

    Do not change any other logic in graph.js. The call must come after the graph data has loaded and the force simulation has started, so that the panel is ready when the UI is interactive.

    Verify by reading the file after the change to confirm both lines are present.
  </action>
  <verify>
    node -e "
    const src = require('fs').readFileSync('./worker/ui/graph.js', 'utf8');
    if (!src.includes('initLogTerminal')) throw new Error('missing initLogTerminal reference');
    if (!src.includes('./modules/log-terminal.js')) throw new Error('missing import');
    console.log('graph.js wiring OK');
    "
    (Run from /Users/ravichillerega/sources/allclear)
  </verify>
  <done>
    graph.js imports initLogTerminal and calls it at the end of the successful project load flow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full log terminal behavior end-to-end</name>
  <what-built>
    Complete log terminal panel:
    - Collapsed bar at the bottom of the graph UI (32px height, "Worker Logs" title)
    - Clicking the header opens the panel to 240px; clicking again collapses it
    - When open, polls /api/logs every 2 seconds and appends new lines
    - Component dropdown (all/worker/http/mcp/scan) filters visible lines
    - Search input filters visible lines by keyword (case-insensitive)
    - Auto-scroll to newest line unless user scrolls up; resumes when scrolled back to bottom
    - Clear button resets the log display
    - DOM never exceeds 500 log line elements
  </what-built>
  <how-to-verify>
    Prerequisites: The worker must be running (it serves both the UI and /api/logs). Phase 24 (/api/logs endpoint) must be implemented.

    1. Open the graph UI in a browser: http://127.0.0.1:PORT/
    2. Confirm a collapsed "Worker Logs" bar appears at the very bottom of the page.
    3. Click the bar header — panel should expand to show the log lines area.
    4. Wait 2-4 seconds — new log lines should appear (if any worker activity is happening).
    5. Open DevTools > Network tab — confirm GET /api/logs requests appear every ~2 seconds.
    6. In the component dropdown, select "http" — only lines with component="http" should remain visible; all others should be hidden.
    7. Select "all" to restore all lines.
    8. Type "scan" in the search box — only lines containing "scan" in their message should remain visible.
    9. Clear the search box — all lines return.
    10. Click "Clear" button — the log lines area should empty.
    11. Scroll up in the log area after several lines have arrived — auto-scroll should STOP.
    12. Scroll back to the bottom — auto-scroll should RESUME on the next poll.
    13. Click the header again — panel should collapse back to 32px.
    14. In DevTools > Elements, after leaving panel open through a scan: confirm `#log-lines` never has more than 500 child elements.
  </how-to-verify>
  <action>Open the graph UI in a browser with the worker running. Follow the verification steps in how-to-verify to confirm all log panel behaviors work correctly.</action>
  <verify>Human visual and interactive verification — follow steps in how-to-verify above.</verify>
  <done>User types "approved" confirming all five Phase 25 success criteria are met.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
End-to-end verification is the checkpoint in Task 2. Automated structural checks:
- graph.js imports and calls initLogTerminal (Task 1 verify step).
- Network tab shows /api/logs polling at 2-second intervals.
- DOM ring buffer confirmed at <= 500 lines after heavy log activity.
</verification>

<success_criteria>
- graph.js wired with initLogTerminal import and call.
- Human verification of all five Phase 25 success criteria from ROADMAP.md:
  1. Collapsed panel visible at bottom; clicking header opens/closes it.
  2. When open, new log lines appear without manual refresh.
  3. Component dropdown filters to a single subsystem.
  4. Search box further filters to lines containing the search string.
  5. Auto-scroll to newest line; pauses when user scrolls up; resumes at bottom.
</success_criteria>

<output>
After completion, create `.planning/phases/25-log-terminal-ui/25-02-SUMMARY.md`
</output>
