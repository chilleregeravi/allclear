---
phase: 37-controls-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/ui/modules/state.js
  - worker/ui/index.html
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
  - CTRL-05
  - CTRL-06
  - CTRL-07

must_haves:
  truths:
    - "Top bar contains only Search input, Project selector, Filters button, and node-count span — no inline protocol checkboxes or legend"
    - "Clicking the Filters button toggles a collapsible panel below the top bar"
    - "Filter panel contains protocol checkboxes, layer checkboxes, mismatch-only toggle, hide-isolated toggle, boundary dropdown, and language dropdown"
    - "state.js exports all new filter fields with correct initial values"
  artifacts:
    - path: "worker/ui/modules/state.js"
      provides: "Extended state with activeLayers, mismatchesOnly, hideIsolated, boundaryFilter, languageFilter, filterPanelOpen"
    - path: "worker/ui/index.html"
      provides: "Minimal top bar + collapsible #filter-panel element with all controls"
  key_links:
    - from: "worker/ui/index.html"
      to: "worker/ui/modules/state.js"
      via: "DOM IDs match state field names used in filter-panel.js (plan 02)"
      pattern: "id=\"filter-panel\""
---

<objective>
Extend state with new filter fields and restructure the HTML/CSS to produce the minimal top bar + collapsible filter panel shell.

Purpose: Establishes the data layer and DOM structure that plans 02 and 03 wire and render against. The filter panel must exist in the DOM before any JS can wire it.

Output: Updated state.js with 6 new fields. Updated index.html with cleaned top bar and new filter panel element.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current state fields (from worker/ui/modules/state.js) -->
```javascript
export const state = {
  graphData: { nodes: [], edges: [], mismatches: [] },
  positions: {},
  selectedNodeId: null,
  blastNodeId: null,
  blastSet: new Set(),
  blastCache: {},
  activeProtocols: new Set(["rest", "grpc", "events", "internal", "sdk"]),
  searchFilter: "",
  forceWorker: null,
  currentProject: null,
  isDragging: false,
  dragNodeId: null,
  isPanning: false,
  panStartX: 0,
  panStartY: 0,
  panStartTransformX: 0,
  panStartTransformY: 0,
  dragStarted: false,
  transform: { x: 0, y: 0, scale: 1 },
  logPanelOpen: false,
  logComponentFilter: "all",
  logSearchFilter: "",
  logLastSince: null,
};
```

<!-- Current top bar HTML (from worker/ui/index.html lines 370-418) -->
Current toolbar has: h1 title, project-select, search, fit-btn, div#filters (inline protocol checkboxes), div#legend, span#node-info.

Target toolbar (per locked design decision): search input, project-select, Filters button. Move protocol checkboxes + other filters into collapsible panel below toolbar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend state.js with filter fields</name>
  <files>worker/ui/modules/state.js</files>
  <read_first>worker/ui/modules/state.js</read_first>
  <action>
Add 6 new fields to the exported `state` object, after the existing `activeProtocols` field:

```javascript
activeLayers: new Set(["services", "libraries", "infra", "external"]),
mismatchesOnly: false,
hideIsolated: false,
boundaryFilter: null,      // string boundary name or null (All)
languageFilter: null,      // string language name or null (All)
filterPanelOpen: false,
```

Do NOT remove or rename any existing fields — renderer.js and interactions.js depend on them. The `activeProtocols` Set stays exactly as is (same initial values). Only add; do not modify.
  </action>
  <verify>
    <automated>node -e "import('./worker/ui/modules/state.js').then(m => { const s = m.state; console.assert(s.activeLayers instanceof Set, 'activeLayers missing'); console.assert(s.activeLayers.has('services'), 'services missing'); console.assert(s.mismatchesOnly === false, 'mismatchesOnly wrong'); console.assert(s.hideIsolated === false, 'hideIsolated wrong'); console.assert(s.boundaryFilter === null, 'boundaryFilter wrong'); console.assert(s.languageFilter === null, 'languageFilter wrong'); console.assert(s.filterPanelOpen === false, 'filterPanelOpen wrong'); console.log('state OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    state.activeLayers is a Set containing "services", "libraries", "infra", "external".
    state.mismatchesOnly is false.
    state.hideIsolated is false.
    state.boundaryFilter is null.
    state.languageFilter is null.
    state.filterPanelOpen is false.
    All pre-existing fields unchanged.
  </acceptance_criteria>
  <done>node import check passes without assertion errors</done>
</task>

<task type="auto">
  <name>Task 2: Restructure index.html — minimal top bar + filter panel shell</name>
  <files>worker/ui/index.html</files>
  <read_first>worker/ui/index.html</read_first>
  <action>
**Top bar (`#toolbar`):** Remove the inline `div#filters` (protocol checkboxes) and `div#legend` entirely. The toolbar must contain exactly:
1. `input#search` (existing, keep as is)
2. `select#project-select` (existing, keep as is)
3. `button#filters-btn` — new button, text "Filters", id="filters-btn"
4. `span#node-info` (existing, keep as is — float right via margin-left: auto)

Remove `button#fit-btn` entirely from the toolbar (fit-to-screen is handled separately; if needed by graph.js, it can remain but hidden — see note below).

NOTE: graph.js references `document.getElementById("fit-btn")`. Keep `#fit-btn` in the DOM but move it out of the visible toolbar: add it as a hidden element (`style="display:none"`) so graph.js does not throw. OR keep it visible — the design allows zoom controls. Decision: keep `#fit-btn` in toolbar between project-select and filters-btn (it is a zoom utility, not a filter). Final toolbar order: search, project-select, fit-btn, filters-btn, node-info.

**New `#filter-panel` element:** Add immediately after `#toolbar`, before `#canvas-container`:

```html
<div id="filter-panel" style="display: none;">
  <!-- populated by filter-panel.js in plan 02 -->
  <div class="filter-section">
    <span class="filter-section-label">Protocol</span>
    <label class="filter-label"><input type="checkbox" data-protocol="rest" checked /> REST</label>
    <label class="filter-label"><input type="checkbox" data-protocol="grpc" checked /> gRPC</label>
    <label class="filter-label"><input type="checkbox" data-protocol="events" checked /> Events</label>
    <label class="filter-label"><input type="checkbox" data-protocol="sdk" checked /> SDK</label>
    <label class="filter-label"><input type="checkbox" data-protocol="internal" checked /> Internal</label>
  </div>
  <div class="filter-section">
    <span class="filter-section-label">Layer</span>
    <label class="filter-label"><input type="checkbox" data-layer="services" checked /> Services</label>
    <label class="filter-label"><input type="checkbox" data-layer="libraries" checked /> Libraries</label>
    <label class="filter-label"><input type="checkbox" data-layer="infra" checked /> Infra</label>
    <label class="filter-label"><input type="checkbox" data-layer="external" checked /> External</label>
  </div>
  <div class="filter-section">
    <span class="filter-section-label">Show</span>
    <label class="filter-label"><input type="checkbox" id="filter-mismatches-only" /> Mismatches only</label>
    <label class="filter-label"><input type="checkbox" id="filter-hide-isolated" /> Hide isolated nodes</label>
  </div>
  <div class="filter-section">
    <span class="filter-section-label">Boundary</span>
    <select id="filter-boundary"><option value="">All</option></select>
  </div>
  <div class="filter-section">
    <span class="filter-section-label">Language</span>
    <select id="filter-language"><option value="">All</option></select>
  </div>
</div>
```

**CSS additions** (add to the `<style>` block):

```css
#filters-btn {
  background: #2d3748;
  border: 1px solid #4a5568;
  color: #e2e8f0;
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  white-space: nowrap;
}
#filters-btn:hover,
#filters-btn.active {
  border-color: #63b3ed;
  color: #63b3ed;
}
#filter-panel {
  background: #1a1f2e;
  border-bottom: 1px solid #2d3748;
  padding: 10px 16px;
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  flex-shrink: 0;
}
.filter-section {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 100px;
}
.filter-section-label {
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #718096;
  margin-bottom: 2px;
}
#filter-boundary,
#filter-language {
  background: #2d3748;
  border: 1px solid #4a5568;
  color: #e2e8f0;
  padding: 3px 6px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  min-width: 80px;
}
```

Remove the existing `.filter-label` and `#filters` CSS rules only if they are no longer referenced after removing `div#filters` from toolbar. Keep `.filter-label` since it is reused in `#filter-panel`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const html = fs.readFileSync('worker/ui/index.html', 'utf8'); console.assert(html.includes('id=\"filter-panel\"'), 'filter-panel missing'); console.assert(html.includes('id=\"filters-btn\"'), 'filters-btn missing'); console.assert(html.includes('data-layer=\"services\"'), 'layer toggle missing'); console.assert(html.includes('id=\"filter-mismatches-only\"'), 'mismatches-only missing'); console.assert(html.includes('id=\"filter-hide-isolated\"'), 'hide-isolated missing'); console.assert(html.includes('id=\"filter-boundary\"'), 'boundary select missing'); console.assert(html.includes('id=\"filter-language\"'), 'language select missing'); console.assert(!html.match(/div id=\"filters\"/), 'old filters div still present'); console.log('HTML structure OK');"</automated>
  </verify>
  <acceptance_criteria>
    Toolbar has no inline protocol checkboxes.
    #filter-panel element exists with all 5 sections: Protocol, Layer, Show, Boundary, Language.
    Layer checkboxes use data-layer attribute with values: services, libraries, infra, external.
    Mismatches only and hide isolated checkboxes have correct IDs.
    Boundary and language selects exist with default "All" option.
    Filters button (#filters-btn) is in toolbar.
    CSS for filter panel, sections, and button is present.
    Opening the page in a browser shows the clean top bar (no sprawling checkboxes).
  </acceptance_criteria>
  <done>HTML structure assertions pass; visual inspection shows minimal toolbar with Filters button</done>
</task>

</tasks>

<verification>
Run both automated verify commands. Then open the UI in browser: the top bar should show Search, Project selector, Fit, Filters button, and node count — nothing else. The filter panel should be invisible (display:none) until wired in plan 02.
</verification>

<success_criteria>
- state.js exports 6 new filter fields with correct types and initial values
- index.html toolbar contains only the approved controls
- #filter-panel exists in DOM with all required child controls
- No existing JS functionality broken (graph.js still finds #fit-btn, #search, #project-select, #node-info, #detail-close)
</success_criteria>

<output>
After completion, create `.planning/phases/37-controls-filters/37-01-SUMMARY.md`
</output>
