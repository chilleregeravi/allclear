---
phase: 120-integration-data-layer-hub-evidence-mode-offline-explicit-sp
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - plugins/arcanon/data/known-externals.yaml
  - tests/known-externals-yaml.bats
autonomous: true
requirements:
  - INT-05

must_haves:
  truths:
    - "plugins/arcanon/data/known-externals.yaml exists at the documented path"
    - "The file is valid YAML — yq parses it without errors"
    - "The top-level structure is { externals: [...] } with at least 20 entries"
    - "Each entry has at minimum: name (string), label (string), category (enum)"
    - "category is one of: api | webhook | observability | storage | auth | infra"
    - "Each entry has either hosts (non-empty array of glob patterns) OR ports (non-empty array of integers) — at least one match signal"
    - "name values are unique across the catalog (no duplicate keys for Phase 121's matcher to disambiguate)"
    - "name values are kebab-case (lowercase + hyphens; no underscores, no camelCase) — matches the established convention for catalog identifiers in the project"
    - "The file includes a top-level header comment block documenting the schema (schema fields, valid category values, glob-vs-regex host pattern rules)"
    - "Phase 120 ships ZERO Node code that loads or consumes the catalog — that is Phase 121's responsibility (per ROADMAP hard constraint)"
    - "tests/known-externals-yaml.bats asserts: file exists, parses as YAML via yq, has >=20 entries, all entries have a name + label + category, all category values are in the enum, all entries have at least one of hosts/ports"
  artifacts:
    - path: "plugins/arcanon/data/known-externals.yaml"
      provides: "Curated catalog of ~20 third-party externals (Stripe, Auth0, OTel Collector, S3, etc.) with a documented schema"
      contains: "externals:"
    - path: "tests/known-externals-yaml.bats"
      provides: "Schema-and-shape validation: file exists, parses, >=20 entries, every entry valid"
      contains: "known-externals.yaml"
  key_links:
    - from: "plugins/arcanon/data/known-externals.yaml externals[].hosts"
      to: "Phase 121 actor-host matching enrichment pass"
      via: "(Phase 121 owns the consumer; Phase 120 ships only the data file)"
      pattern: "hosts"
    - from: "plugins/arcanon/data/known-externals.yaml externals[].name + label"
      to: "Phase 121 graph UI label rendering for catalog-matched actors"
      via: "(Phase 121 owns the renderer; Phase 120 ships only the data)"
      pattern: "label"
---

<objective>
Ship the `known-externals.yaml` catalog file as data, with bats coverage that asserts the schema is well-formed and the catalog has at least 20 entries. **No Node code change. No scan-pipeline change. No graph UI change.** Phase 121 consumes the catalog; this plan ships only the data so Phase 121 has something concrete to consume.

Per the hard constraint in the orchestrator brief: "`known-externals.yaml` is data, not code — Phase 121 consumes it. Don't add Node code to ingest the catalog in Phase 120." This plan respects that boundary strictly.

Schema (decided in RESEARCH §5):
```yaml
externals:
  - name: <kebab-case unique id>
    label: <human-friendly display name>
    category: api | webhook | observability | storage | auth | infra
    hosts: [<glob-style host patterns, e.g. "api.stripe.com" or "*.auth0.com">]   # optional, but at least one of hosts/ports required
    ports: [<integers>]                                                            # optional
    evidence_url: <docs URL>                                                       # optional metadata
```

Output:
- `plugins/arcanon/data/known-externals.yaml` with header comment, schema doc, and 20 curated entries
- `tests/known-externals-yaml.bats` with 6+ schema-validation @test blocks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-integration-data-layer-hub-evidence-mode-offline-explicit-sp/120-RESEARCH.md

<interfaces>
<!-- Schema decided in RESEARCH §5; 20-entry catalog table in RESEARCH §5. -->

The directory `plugins/arcanon/data/` does not exist yet. Mkdir it as part of file creation.

The 20 entries to ship (from RESEARCH §5):

| name | label | category | hosts / ports |
| --- | --- | --- | --- |
| stripe | Stripe API | api | hosts: [api.stripe.com] |
| auth0 | Auth0 | auth | hosts: [*.auth0.com] |
| okta | Okta | auth | hosts: [*.okta.com, *.oktapreview.com] |
| dex | Dex Identity Provider | auth | ports: [5556, 5557] |
| otel-collector | OpenTelemetry Collector | observability | ports: [4317, 4318] |
| s3 | AWS S3 | storage | hosts: [*.s3.amazonaws.com, s3.*.amazonaws.com] |
| azure-blob | Azure Blob Storage | storage | hosts: [*.blob.core.windows.net] |
| gcs | Google Cloud Storage | storage | hosts: [storage.googleapis.com, *.storage.googleapis.com] |
| github | GitHub API | api | hosts: [api.github.com] |
| slack-webhooks | Slack Webhooks | webhook | hosts: [hooks.slack.com] |
| pagerduty | PagerDuty | api | hosts: [api.pagerduty.com, events.pagerduty.com] |
| sentry | Sentry | observability | hosts: [sentry.io, *.ingest.sentry.io] |
| datadog | Datadog | observability | hosts: [*.datadoghq.com, *.datadoghq.eu] |
| twilio | Twilio | api | hosts: [api.twilio.com] |
| sendgrid | SendGrid | api | hosts: [api.sendgrid.com] |
| mailgun | Mailgun | api | hosts: [api.mailgun.net, api.eu.mailgun.net] |
| cloudflare | Cloudflare | infra | hosts: [api.cloudflare.com] |
| segment | Segment | observability | hosts: [api.segment.io, cdn.segment.com] |
| mixpanel | Mixpanel | observability | hosts: [api.mixpanel.com] |
| aws-lambda | AWS Lambda | infra | hosts: [lambda.*.amazonaws.com] |

Header comment block to include (top of file):
```yaml
# known-externals.yaml — catalog of common third-party hosts and services
# that frequently appear as actors in service dependency graphs.
#
# Schema (Phase 120 — INT-05; consumed by Phase 121 — INT-06..08):
#
#   externals: list of catalog entries
#     - name: kebab-case unique identifier (used as dictionary key by consumers)
#       label: human-friendly display name shown in UI / /arcanon:list output
#       category: one of [api, webhook, observability, storage, auth, infra]
#       hosts: optional list of glob-style hostname patterns (e.g. "*.auth0.com",
#              "api.stripe.com"). NOT regex — glob: "*" matches one DNS label
#              ("*.foo.com" matches "a.foo.com" but not "a.b.foo.com").
#       ports: optional list of integer port numbers, for non-HTTP services
#              (e.g. OpenTelemetry Collector's 4317/4318 gRPC/HTTP).
#       evidence_url: optional documentation URL for the entry, surfaced in UI
#                     as a link from the labeled actor.
#
# At least one of `hosts` or `ports` is required per entry — entries without
# any match signal cannot be matched and are useless to consumers.
#
# User extension: arcanon.config.json may carry an `external_labels` key with
# the same shape; user entries are MERGED with this catalog and take precedence
# on `name` collision (Phase 121 INT-07).
#
# Adding entries: add at the bottom, keep alphabetical-by-name within category
# is a soft preference. Update the entry count in the test (tests/known-externals-yaml.bats).
externals:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/known-externals-yaml.bats first (RED), then create the YAML file (GREEN)</name>
  <files>
    tests/known-externals-yaml.bats,
    plugins/arcanon/data/known-externals.yaml
  </files>
  <behavior>
    - Test 1 (file exists): `[ -f plugins/arcanon/data/known-externals.yaml ]`.
    - Test 2 (valid YAML): `yq '.externals' plugins/arcanon/data/known-externals.yaml` exits 0 (parsable).
    - Test 3 (≥20 entries): `yq '.externals | length' plugins/arcanon/data/known-externals.yaml` returns an integer >= 20.
    - Test 4 (every entry has name + label + category): for each entry, `yq '.externals[$i].name'`, `.label`, `.category` are all non-null strings.
    - Test 5 (category enum): every `.externals[].category` is one of `api`, `webhook`, `observability`, `storage`, `auth`, `infra`.
    - Test 6 (at least one match signal): every entry has either `hosts` (non-empty array) OR `ports` (non-empty array).
    - Test 7 (kebab-case names): every `.externals[].name` matches `^[a-z][a-z0-9-]*$` — lowercase, no underscores, no camelCase.
    - Test 8 (unique names): no duplicate `name` values across the catalog (`yq '.externals[].name' | sort | uniq -d` returns nothing).
    - Test 9 (header comment present): `head -5 known-externals.yaml | grep -F '# known-externals.yaml — catalog'` succeeds.
  </behavior>
  <action>
    Step 1 — Write `tests/known-externals-yaml.bats` FIRST. Use `yq` (already a dependency for drift-openapi.sh) for YAML parsing. Tests use bash array iteration over the entry count. Mirror the existing bats structure (`load test_helper`, `setup_file` resolves the file path):

    ```bash
    #!/usr/bin/env bats
    # tests/known-externals-yaml.bats — INT-05

    load test_helper/common
    PLUGIN_ROOT="${BATS_TEST_DIRNAME}/../plugins/arcanon"
    CATALOG="${PLUGIN_ROOT}/data/known-externals.yaml"
    VALID_CATEGORIES="api webhook observability storage auth infra"

    setup() {
      command -v yq >/dev/null 2>&1 || skip "yq not installed — required for catalog tests"
    }

    @test "INT-05: known-externals.yaml file exists at the documented path" {
      [ -f "$CATALOG" ]
    }

    @test "INT-05: known-externals.yaml is valid YAML and has an externals top-level key" {
      run yq '.externals' "$CATALOG"
      [ "$status" -eq 0 ]
      [[ "$output" != "null" ]]
    }

    @test "INT-05: known-externals.yaml has at least 20 entries" {
      run yq '.externals | length' "$CATALOG"
      [ "$status" -eq 0 ]
      [ "$output" -ge 20 ]
    }

    @test "INT-05: every entry has name + label + category" {
      count=$(yq '.externals | length' "$CATALOG")
      for i in $(seq 0 $((count - 1))); do
        name=$(yq ".externals[$i].name" "$CATALOG")
        label=$(yq ".externals[$i].label" "$CATALOG")
        category=$(yq ".externals[$i].category" "$CATALOG")
        [[ "$name" != "null" && -n "$name" ]] || { echo "entry $i missing name"; return 1; }
        [[ "$label" != "null" && -n "$label" ]] || { echo "entry $i missing label"; return 1; }
        [[ "$category" != "null" && -n "$category" ]] || { echo "entry $i missing category"; return 1; }
      done
    }

    @test "INT-05: every entry's category is in the documented enum" {
      count=$(yq '.externals | length' "$CATALOG")
      for i in $(seq 0 $((count - 1))); do
        category=$(yq ".externals[$i].category" "$CATALOG")
        if ! [[ " $VALID_CATEGORIES " =~ " $category " ]]; then
          echo "entry $i has invalid category: $category (must be one of: $VALID_CATEGORIES)"
          return 1
        fi
      done
    }

    @test "INT-05: every entry has at least one match signal (hosts or ports)" {
      count=$(yq '.externals | length' "$CATALOG")
      for i in $(seq 0 $((count - 1))); do
        hosts_len=$(yq ".externals[$i].hosts | length // 0" "$CATALOG")
        ports_len=$(yq ".externals[$i].ports | length // 0" "$CATALOG")
        if [[ "$hosts_len" -eq 0 && "$ports_len" -eq 0 ]]; then
          name=$(yq ".externals[$i].name" "$CATALOG")
          echo "entry $i ($name) has no hosts and no ports — at least one is required"
          return 1
        fi
      done
    }

    @test "INT-05: all names are kebab-case" {
      count=$(yq '.externals | length' "$CATALOG")
      for i in $(seq 0 $((count - 1))); do
        name=$(yq ".externals[$i].name" "$CATALOG")
        if ! [[ "$name" =~ ^[a-z][a-z0-9-]*$ ]]; then
          echo "entry $i name '$name' is not kebab-case (lowercase, alphanumeric + hyphens only)"
          return 1
        fi
      done
    }

    @test "INT-05: all names are unique" {
      dupes=$(yq '.externals[].name' "$CATALOG" | sort | uniq -d)
      [ -z "$dupes" ] || { echo "duplicate names: $dupes"; return 1; }
    }

    @test "INT-05: file has the documented header comment block" {
      run grep -F '# known-externals.yaml — catalog' "$CATALOG"
      [ "$status" -eq 0 ]
      run grep -F '# Schema (Phase 120 — INT-05;' "$CATALOG"
      [ "$status" -eq 0 ]
    }
    ```

    Step 2 — Run the bats file. All 9 tests should FAIL with "file not found" (RED — file doesn't exist yet).

    Step 3 — Create `plugins/arcanon/data/` directory.

    Step 4 — Create `plugins/arcanon/data/known-externals.yaml` with the header comment from the interface spec, then the 20 entries from the table:

    ```yaml
    # known-externals.yaml — catalog of common third-party hosts and services
    # that frequently appear as actors in service dependency graphs.
    #
    # Schema (Phase 120 — INT-05; consumed by Phase 121 — INT-06..08):
    #
    #   externals: list of catalog entries
    #     - name: kebab-case unique identifier (used as dictionary key by consumers)
    #       label: human-friendly display name shown in UI / /arcanon:list output
    #       category: one of [api, webhook, observability, storage, auth, infra]
    #       hosts: optional list of glob-style hostname patterns (e.g. "*.auth0.com",
    #              "api.stripe.com"). NOT regex — glob: "*" matches one DNS label
    #              ("*.foo.com" matches "a.foo.com" but not "a.b.foo.com").
    #       ports: optional list of integer port numbers, for non-HTTP services
    #              (e.g. OpenTelemetry Collector's 4317/4318 gRPC/HTTP).
    #       evidence_url: optional documentation URL for the entry, surfaced in UI
    #                     as a link from the labeled actor.
    #
    # At least one of `hosts` or `ports` is required per entry — entries without
    # any match signal cannot be matched and are useless to consumers.
    #
    # User extension: arcanon.config.json may carry an `external_labels` key with
    # the same shape; user entries are MERGED with this catalog and take precedence
    # on `name` collision (Phase 121 INT-07).
    #
    # Adding entries: keep alphabetical-by-name within category as a soft preference.
    # When adding, update the entry count threshold in tests/known-externals-yaml.bats
    # if you want a strict floor.
    externals:
      # ── api ────────────────────────────────────────────────────────────────
      - name: cloudflare
        label: Cloudflare
        category: infra
        hosts: ["api.cloudflare.com"]
        evidence_url: https://developers.cloudflare.com/api/

      - name: github
        label: GitHub API
        category: api
        hosts: ["api.github.com"]
        evidence_url: https://docs.github.com/en/rest

      - name: mailgun
        label: Mailgun
        category: api
        hosts: ["api.mailgun.net", "api.eu.mailgun.net"]
        evidence_url: https://documentation.mailgun.com/en/latest/api_reference.html

      - name: pagerduty
        label: PagerDuty
        category: api
        hosts: ["api.pagerduty.com", "events.pagerduty.com"]
        evidence_url: https://developer.pagerduty.com/api-reference/

      - name: sendgrid
        label: SendGrid
        category: api
        hosts: ["api.sendgrid.com"]
        evidence_url: https://docs.sendgrid.com/api-reference

      - name: stripe
        label: Stripe API
        category: api
        hosts: ["api.stripe.com"]
        evidence_url: https://stripe.com/docs/api

      - name: twilio
        label: Twilio
        category: api
        hosts: ["api.twilio.com"]
        evidence_url: https://www.twilio.com/docs/usage/api

      # ── auth ───────────────────────────────────────────────────────────────
      - name: auth0
        label: Auth0
        category: auth
        hosts: ["*.auth0.com"]
        evidence_url: https://auth0.com/docs/api

      - name: dex
        label: Dex Identity Provider
        category: auth
        ports: [5556, 5557]
        evidence_url: https://dexidp.io/docs/

      - name: okta
        label: Okta
        category: auth
        hosts: ["*.okta.com", "*.oktapreview.com"]
        evidence_url: https://developer.okta.com/docs/reference/

      # ── infra ──────────────────────────────────────────────────────────────
      - name: aws-lambda
        label: AWS Lambda
        category: infra
        hosts: ["lambda.*.amazonaws.com"]
        evidence_url: https://docs.aws.amazon.com/lambda/

      # ── observability ──────────────────────────────────────────────────────
      - name: datadog
        label: Datadog
        category: observability
        hosts: ["*.datadoghq.com", "*.datadoghq.eu"]
        evidence_url: https://docs.datadoghq.com/api/

      - name: mixpanel
        label: Mixpanel
        category: observability
        hosts: ["api.mixpanel.com"]
        evidence_url: https://developer.mixpanel.com/reference/overview

      - name: otel-collector
        label: OpenTelemetry Collector
        category: observability
        ports: [4317, 4318]
        evidence_url: https://opentelemetry.io/docs/collector/

      - name: segment
        label: Segment
        category: observability
        hosts: ["api.segment.io", "cdn.segment.com"]
        evidence_url: https://segment.com/docs/connections/sources/

      - name: sentry
        label: Sentry
        category: observability
        hosts: ["sentry.io", "*.ingest.sentry.io"]
        evidence_url: https://docs.sentry.io/api/

      # ── storage ────────────────────────────────────────────────────────────
      - name: azure-blob
        label: Azure Blob Storage
        category: storage
        hosts: ["*.blob.core.windows.net"]
        evidence_url: https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api

      - name: gcs
        label: Google Cloud Storage
        category: storage
        hosts: ["storage.googleapis.com", "*.storage.googleapis.com"]
        evidence_url: https://cloud.google.com/storage/docs/json_api

      - name: s3
        label: AWS S3
        category: storage
        hosts: ["*.s3.amazonaws.com", "s3.*.amazonaws.com"]
        evidence_url: https://docs.aws.amazon.com/AmazonS3/latest/API/Welcome.html

      # ── webhook ────────────────────────────────────────────────────────────
      - name: slack-webhooks
        label: Slack Webhooks
        category: webhook
        hosts: ["hooks.slack.com"]
        evidence_url: https://api.slack.com/messaging/webhooks
    ```

    Step 5 — Run the bats file. All 9 tests should now PASS (GREEN).

    Step 6 — Confirm Phase 120 ships ZERO Node code for catalog ingestion. Search for any new imports or references to known-externals.yaml in JS files:
    ```
    grep -rn 'known-externals' plugins/arcanon/worker plugins/arcanon/lib plugins/arcanon/scripts 2>&1
    ```
    Expected: no hits. If any hit appears, that's a scope violation — Phase 121 owns the consumer.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/ligamen && bats tests/known-externals-yaml.bats && grep -rn 'known-externals' plugins/arcanon/worker plugins/arcanon/lib plugins/arcanon/scripts 2>&1; test $? -ne 0</automated>
  </verify>
  <done>
    plugins/arcanon/data/known-externals.yaml exists with header comment + 20 entries spanning all 6 categories. tests/known-externals-yaml.bats has 9 tests, all green. Zero Node code references the catalog (Phase 121's responsibility).
  </done>
</task>

</tasks>

<verification>

## End-to-end verification

After Task 1 completes:

```
cd /Users/ravichillerega/sources/ligamen
# 1. Schema validation tests
bats tests/known-externals-yaml.bats
# 2. Confirm scope boundary — NO Node code touched the catalog in Phase 120
grep -rn 'known-externals' plugins/arcanon/worker plugins/arcanon/lib plugins/arcanon/scripts
# (Expected: no matches. The grep should exit 1.)
# 3. Full bats suite — confirm no test broke
bats tests/
```

All three checks must pass. The negative grep is the load-bearing scope-boundary check; if Phase 120 accidentally shipped a loader, Phase 121 will collide.

## Manual smoke

1. Open `plugins/arcanon/data/known-externals.yaml` and visually inspect the header comment.
2. Run `yq '.externals[] | .name + " (" + .category + ")"' plugins/arcanon/data/known-externals.yaml` and confirm 20 entries print, each with a name + category.

</verification>

<success_criteria>

1. **Catalog file exists at the documented path.** `plugins/arcanon/data/known-externals.yaml` shipped with header comment + ≥20 entries.

2. **Schema validation comprehensive.** 9 bats tests cover existence, parseability, entry count, required-field presence, category enum, match-signal presence, name format, name uniqueness, header comment.

3. **Six categories represented.** Catalog spans `api`, `webhook`, `observability`, `storage`, `auth`, `infra` — proves the enum is realistic.

4. **Phase 121 boundary respected.** Grep for `known-externals` across `plugins/arcanon/worker`, `plugins/arcanon/lib`, `plugins/arcanon/scripts` returns no hits. The catalog is data-only in Phase 120; consumers land in Phase 121.

5. **Schema is forgiving for catalog editors.** Glob-style host patterns (not regex), optional `evidence_url`, optional `ports`. Future entries can be added without touching code.

</success_criteria>

<output>
After completion, create `.planning/phases/120-integration-data-layer-hub-evidence-mode-offline-explicit-sp/120-03-SUMMARY.md` describing:
- What changed (one new YAML file at `plugins/arcanon/data/known-externals.yaml` with 20 entries; one new bats file at `tests/known-externals-yaml.bats` with 9 tests)
- Decision references (RESEARCH §5 schema rationale: glob hosts vs regex, category enum choice, `evidence_url` optional, top-level `externals:` key for future-extensibility)
- Test summary (9 schema-validation bats tests; 0 Node code shipped; 0 regressions)
- Phase 121 handoff: Phase 121 reads this catalog, implements glob-pattern matching, applies labels to actor records, and merges `arcanon.config.json` `external_labels` user extensions. The schema chosen here is intentionally forgiving to give Phase 121 room to evolve the matcher (e.g., add port matching for OTel Collector / Dex without breaking Stripe-style host-only entries).
</output>
