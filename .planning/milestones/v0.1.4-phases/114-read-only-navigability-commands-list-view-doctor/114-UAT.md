---
status: testing
phase: 114-read-only-navigability-commands-list-view-doctor
source:
  - 114-01-SUMMARY.md
  - 114-02-SUMMARY.md
  - 114-03-SUMMARY.md
started: 2026-04-25T20:30:00Z
updated: 2026-04-25T20:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Cold Start Smoke Test
expected: |
  Kill the Arcanon worker (e.g., `pkill -f 'plugins/arcanon/worker/index.js'` or `/arcanon:update --kill`).
  Then run `/arcanon:list` in a project directory that has been scanned (i.e. has `arcanon.config.json` and a populated `impact-map.db`).
  The markdown wrapper should auto-start the worker, then print the 5-line overview without errors.
  No `worker unreachable` message, no stack traces.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Kill the worker, then run `/arcanon:list` in a scanned project. Worker auto-starts, command prints 5-line overview with no errors.
result: pending

### 2. `/arcanon:list` Happy Path
expected: In an Arcanon-scanned directory, `/arcanon:list` prints 5 lines — Linked repos count, Services partitioned by type, Connections partitioned by confidence (high/medium/low), External actors count, Hub status (synced or local-only).
result: pending

### 3. `/arcanon:list` Silent in Non-Arcanon Directory
expected: `cd /tmp && /arcanon:list` (or any directory without `impact-map.db`) exits 0 with no output. No error, no banner, no "not an Arcanon project" message — simply nothing.
result: pending

### 4. `/arcanon:view` Opens Graph UI
expected: In an Arcanon-scanned directory, `/arcanon:view` launches the default browser to the graph UI (typically `http://localhost:<port>/graph`). Same effect as `/arcanon:map view` — verify both still work and produce identical results.
result: pending

### 5. `/arcanon:doctor` Happy Path (All 8 Checks PASS)
expected: In a healthy environment (worker reachable, schema at head=016, hub creds optional), `/arcanon:doctor` prints a table of 8 numbered checks, each with PASS, exits 0. Checks: 1) worker reachable, 2) version match, 3) schema head, 4) config + linked repos, 5) data dir writable, 6) DB integrity (PRAGMA quick_check), 7) MCP liveness, 8) hub credentials (PASS or SKIP).
result: pending

### 6. `/arcanon:doctor --json` Structured Output
expected: `/arcanon:doctor --json` emits a JSON object with `{checks: [...8 items], summary: {pass, fail, warn, skip}, exit_code: 0}`. Each check has `id` (1-8), `name`, `status` (PASS/FAIL/WARN/SKIP), `detail` (string), `elapsed_ms` (number).
result: pending

### 7. `/arcanon:doctor` FAIL Path Exits 1
expected: Induce a critical failure (e.g., `chmod 000 $ARCANON_DATA_DIR/projects/<hash>/` to break check 5 data-dir write, or run from a directory without an Arcanon config to break check 4). `/arcanon:doctor` reports the failing check as FAIL, exits with code 1. Restore perms after.
result: pending

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0

## Gaps

[none yet]
