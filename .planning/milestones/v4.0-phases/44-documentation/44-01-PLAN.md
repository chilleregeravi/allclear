---
phase: 44-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README.md install instructions reference ligamen repository, @ligamen/cli package, and /ligamen:* commands"
    - "README.md contains no remaining references to allclear, AllClear, or ALLCLEAR (except in the Apache-2.0 license line which has no product name)"
    - "README.md machine settings section shows ~/.ligamen/settings.json and LIGAMEN_* variable names"
    - "README.md config section shows ligamen.config.json as the project config filename"
  artifacts:
    - path: "README.md"
      provides: "Updated user-facing README with full Ligamen branding"
      contains: "Ligamen"
  key_links:
    - from: "README.md"
      to: "ligamen repository"
      via: "plugin install command"
      pattern: "claude plugin install ligamen"
    - from: "README.md"
      to: "@ligamen/cli"
      via: "npm package reference"
      pattern: "@ligamen/cli"
---

<objective>
Rename all AllClear/allclear/ALLCLEAR references in README.md to Ligamen/ligamen/LIGAMEN.

Purpose: README is the primary user-facing entry point; it must reflect the new product name for installation, command usage, and configuration.
Output: README.md fully updated with Ligamen branding.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename README.md — product name, install, commands, config</name>
  <read_first>
    README.md
  </read_first>
  <files>README.md</files>
  <action>
Rewrite README.md applying the following substitutions throughout the entire file. Apply every substitution — do not skip any occurrence:

PRODUCT NAME:
- "# AllClear" → "# Ligamen"
- "AllClear is" → "Ligamen is"
- "Install AllClear" → "Install Ligamen"
- "AllClear as a Claude Code plugin" → "Ligamen as a Claude Code plugin"
- "registers AllClear globally" → "registers Ligamen globally"
- "AllClear runs" → "Ligamen runs"
- "AllClear hooks" → "Ligamen hooks"
- "AllClear can sync" → "Ligamen can sync"
- "AllClear falls back" → "Ligamen falls back"
- "AllClear logs" → "Ligamen logs"
- "AllClear itself" → "Ligamen itself"
- Any remaining standalone "AllClear" → "Ligamen"

PLUGIN INSTALL COMMAND:
- "claude plugin install allclear --repo chilleregeravi/allclear" → "claude plugin install ligamen --repo chilleregeravi/ligamen"

GIT CLONE URL:
- "git clone https://github.com/chilleregeravi/allclear.git" → "git clone https://github.com/chilleregeravi/ligamen.git"
- "cd allclear" → "cd ligamen"
- "claude --plugin-dir /path/to/allclear" → "claude --plugin-dir /path/to/ligamen"

SLASH COMMANDS (every occurrence in the file):
- "/allclear:quality-gate" → "/ligamen:quality-gate"
- "/allclear:map" → "/ligamen:map"
- "/allclear:cross-impact" → "/ligamen:cross-impact"
- "/allclear:drift" → "/ligamen:drift"
- "/allclear:pulse" → "/ligamen:pulse"
- "/allclear:deploy-verify" → "/ligamen:deploy-verify"

CONFIG FILENAME:
- "allclear.config.json" (all occurrences, including code block and table) → "ligamen.config.json"

MACHINE SETTINGS PATH:
- "~/.allclear/settings.json" (all occurrences) → "~/.ligamen/settings.json"
- "~/.allclear/" (directory reference) → "~/.ligamen/"

ENVIRONMENT VARIABLES (keys in the table and in JSON code blocks):
- "ALLCLEAR_WORKER_PORT" → "LIGAMEN_WORKER_PORT"
- "ALLCLEAR_LOG_LEVEL" → "LIGAMEN_LOG_LEVEL"
- "ALLCLEAR_CHROMA_MODE" → "LIGAMEN_CHROMA_MODE"
- "ALLCLEAR_CHROMA_HOST" → "LIGAMEN_CHROMA_HOST"
- "ALLCLEAR_CHROMA_PORT" → "LIGAMEN_CHROMA_PORT"
- "ALLCLEAR_CHROMA_SSL" → "LIGAMEN_CHROMA_SSL"
- "ALLCLEAR_CHROMA_API_KEY" → "LIGAMEN_CHROMA_API_KEY"
- "ALLCLEAR_CHROMA_TENANT" → "LIGAMEN_CHROMA_TENANT"
- "ALLCLEAR_CHROMA_DATABASE" → "LIGAMEN_CHROMA_DATABASE"
- "ALLCLEAR_DISABLE_FORMAT" → "LIGAMEN_DISABLE_FORMAT"
- "ALLCLEAR_DISABLE_LINT" → "LIGAMEN_DISABLE_LINT"
- "ALLCLEAR_DISABLE_GUARD" → "LIGAMEN_DISABLE_GUARD"
- "ALLCLEAR_DISABLE_SESSION_START" → "LIGAMEN_DISABLE_SESSION_START"

NPM PACKAGE:
- "@allclear/cli" → "@ligamen/cli" (if it appears anywhere)

MCP SERVER NAME IN JSON SNIPPET:
- "allclear-impact" → "ligamen-impact"

PLUGIN NAMESPACE IN TEXT:
- "(plugin:allclear)" → "(plugin:ligamen)"

Do NOT change: the Apache-2.0 license line (it contains no product name), the ChromaDB documentation references (trychroma.com URL, docker image names, pip package), the Claude Code documentation URL, or the links to docs/ files (those are correct relative paths with no allclear in the path segment).
  </action>
  <verify>
    <automated>grep -c "allclear\|AllClear\|ALLCLEAR" README.md && echo "FAIL — allclear references remain" || echo "PASS — no allclear references"</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "allclear" README.md returns exit code 1 (no matches)
    - grep "ligamen" README.md returns at least 20 matches
    - grep "claude plugin install ligamen" README.md returns a match
    - grep "ligamen.config.json" README.md returns at least 2 matches
    - grep "~/.ligamen/" README.md returns at least 2 matches
    - grep "LIGAMEN_WORKER_PORT" README.md returns a match
    - grep "/ligamen:quality-gate" README.md returns a match
    - grep "@ligamen/cli" README.md returns a match (if npm package referenced)
    - grep "ligamen-impact" README.md returns a match (in MCP JSON snippet)
  </acceptance_criteria>
  <done>README.md contains zero allclear/AllClear/ALLCLEAR references and all user-facing install instructions, command references, config filenames, env vars, and paths use the Ligamen namespace.</done>
</task>

</tasks>

<verification>
Run: grep -i "allclear" README.md
Expected: no output (exit code 1)

Run: grep -c "ligamen\|Ligamen\|LIGAMEN" README.md
Expected: 25+ matches
</verification>

<success_criteria>
README.md install instructions reference the ligamen repository, @ligamen/cli package, and /ligamen:* commands throughout — no allclear references remain.
</success_criteria>

<output>
After completion, create `.planning/phases/44-documentation/44-01-SUMMARY.md` following the summary template.
</output>
