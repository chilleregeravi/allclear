---
phase: 44-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/commands.md
  - docs/configuration.md
  - docs/hooks.md
  - docs/architecture.md
  - docs/service-map.md
  - docs/development.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "All docs/ files reference /ligamen:* commands exclusively — no /allclear:* commands remain"
    - "docs/configuration.md shows ligamen.config.json, ~/.ligamen/settings.json, and LIGAMEN_* env vars"
    - "docs/hooks.md shows LIGAMEN_DISABLE_* variable names"
    - "docs/architecture.md shows (plugin:ligamen) namespacing and ligamen directory name"
    - "docs/service-map.md shows ligamen-impact MCP server name"
    - "docs/development.md shows ligamen git clone URL and /ligamen:quality-gate command"
    - "Planning docs use Ligamen as the product name wherever the product name appears"
    - "No docs/ or planning/ file contains allclear, AllClear, or ALLCLEAR"
  artifacts:
    - path: "docs/commands.md"
      provides: "Commands reference with /ligamen:* invocations"
      contains: "/ligamen:"
    - path: "docs/configuration.md"
      provides: "Configuration reference with ligamen.config.json and LIGAMEN_* vars"
      contains: "ligamen.config.json"
    - path: "docs/hooks.md"
      provides: "Hooks reference with LIGAMEN_DISABLE_* variables"
      contains: "LIGAMEN_DISABLE_FORMAT"
    - path: "docs/architecture.md"
      provides: "Architecture reference with ligamen directory and plugin namespace"
      contains: "plugin:ligamen"
    - path: "docs/service-map.md"
      provides: "Service map reference with ligamen-impact MCP name"
      contains: "ligamen-impact"
    - path: "docs/development.md"
      provides: "Development guide with ligamen repo URL"
      contains: "chilleregeravi/ligamen"
    - path: ".planning/PROJECT.md"
      provides: "Planning doc with Ligamen as product name"
      contains: "Ligamen"
    - path: ".planning/MILESTONES.md"
      provides: "Milestones doc with Ligamen product name"
      contains: "Ligamen"
  key_links:
    - from: "docs/commands.md"
      to: "slash commands"
      via: "command invocation syntax"
      pattern: "/ligamen:quality-gate"
    - from: "docs/configuration.md"
      to: "config file"
      via: "config filename reference"
      pattern: "ligamen.config.json"
    - from: ".planning/PROJECT.md"
      to: "product identity"
      via: "project name heading and milestone section"
      pattern: "# Ligamen"
---

<objective>
Rename all AllClear/allclear/ALLCLEAR references in the six docs/ files and three planning docs to Ligamen/ligamen/LIGAMEN.

Purpose: All user-facing documentation must consistently describe the plugin as Ligamen.
Output: Six docs/ files and three .planning/ docs updated with Ligamen branding throughout.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all six docs/ files</name>
  <read_first>
    docs/commands.md
    docs/configuration.md
    docs/hooks.md
    docs/architecture.md
    docs/service-map.md
    docs/development.md
  </read_first>
  <files>docs/commands.md, docs/configuration.md, docs/hooks.md, docs/architecture.md, docs/service-map.md, docs/development.md</files>
  <action>
Update each docs/ file applying the substitutions below. Read each file fully before editing to ensure no occurrence is missed.

SUBSTITUTIONS TO APPLY IN ALL SIX FILES:

PRODUCT NAME:
- "AllClear" (any standalone occurrence as product name) → "Ligamen"
- "allclear" (as product directory name: "allclear/", "to allclear", "path/to/allclear") → "ligamen"

SLASH COMMANDS (apply in all files, especially commands.md and service-map.md):
- "/allclear:quality-gate" → "/ligamen:quality-gate"
- "/allclear:map" → "/ligamen:map"
- "/allclear:cross-impact" → "/ligamen:cross-impact"
- "/allclear:drift" → "/ligamen:drift"
- "/allclear:pulse" → "/ligamen:pulse"
- "/allclear:deploy-verify" → "/ligamen:deploy-verify"
- Any remaining "/allclear:<anything>" → "/ligamen:<anything>"

PLUGIN NAMESPACE (architecture.md):
- "(plugin:allclear)" → "(plugin:ligamen)"
- "commands/ — user types `/allclear:<name>`" → "commands/ — user types `/ligamen:<name>`"

CONFIG FILENAME (configuration.md):
- "allclear.config.json" → "ligamen.config.json"

MACHINE SETTINGS PATH (configuration.md):
- "~/.allclear/settings.json" → "~/.ligamen/settings.json"
- "~/.allclear/" (directory references, including data dir tree) → "~/.ligamen/"
- "~/.allclear" (without trailing slash) → "~/.ligamen"
- The data directory tree section showing "~/.allclear/" must show "~/.ligamen/"

ENVIRONMENT VARIABLES (configuration.md and hooks.md):
- "ALLCLEAR_WORKER_PORT" → "LIGAMEN_WORKER_PORT"
- "ALLCLEAR_WORKER_HOST" → "LIGAMEN_WORKER_HOST"
- "ALLCLEAR_DATA_DIR" → "LIGAMEN_DATA_DIR"
- "ALLCLEAR_LOG_LEVEL" → "LIGAMEN_LOG_LEVEL"
- "ALLCLEAR_CHROMA_MODE" → "LIGAMEN_CHROMA_MODE"
- "ALLCLEAR_CHROMA_HOST" → "LIGAMEN_CHROMA_HOST"
- "ALLCLEAR_CHROMA_PORT" → "LIGAMEN_CHROMA_PORT"
- "ALLCLEAR_CHROMA_SSL" → "LIGAMEN_CHROMA_SSL"
- "ALLCLEAR_CHROMA_API_KEY" → "LIGAMEN_CHROMA_API_KEY"
- "ALLCLEAR_CHROMA_TENANT" → "LIGAMEN_CHROMA_TENANT"
- "ALLCLEAR_CHROMA_DATABASE" → "LIGAMEN_CHROMA_DATABASE"
- "ALLCLEAR_DISABLE_FORMAT" → "LIGAMEN_DISABLE_FORMAT"
- "ALLCLEAR_DISABLE_LINT" → "LIGAMEN_DISABLE_LINT"
- "ALLCLEAR_DISABLE_GUARD" → "LIGAMEN_DISABLE_GUARD"
- "ALLCLEAR_DISABLE_SESSION_START" → "LIGAMEN_DISABLE_SESSION_START"
- "ALLCLEAR_LINT_THROTTLE" → "LIGAMEN_LINT_THROTTLE"
- "ALLCLEAR_EXTRA_BLOCKED" → "LIGAMEN_EXTRA_BLOCKED"

MCP SERVER NAME (service-map.md):
- "allclear-impact" → "ligamen-impact"
- The .mcp.json snippet: `"allclear-impact"` key → `"ligamen-impact"`
- The args path in the .mcp.json snippet: `"<path-to-allclear>/worker/mcp/server.js"` → `"<path-to-ligamen>/worker/mcp/server.js"`

GIT CLONE URL (development.md):
- "git clone https://github.com/chilleregeravi/allclear.git" → "git clone https://github.com/chilleregeravi/ligamen.git"
- "cd allclear" → "cd ligamen"
- "claude --plugin-dir /path/to/allclear" → "claude --plugin-dir /path/to/ligamen"

ARCHITECTURE DIRECTORY REFERENCE (architecture.md):
- The project structure tree shows "allclear/" at the top — rename to "ligamen/"
- `allclear.config.json` in the tree or text → `ligamen.config.json`

Do NOT change: ChromaDB documentation (trychroma.com URL, docker image names chromadb/chroma, pip package), Claude Code documentation URL, or relative links between docs/ files (hooks.md, commands.md, etc. — they are correct).

File-specific notes:
- docs/commands.md: All six command blocks plus inline invocations
- docs/configuration.md: config filename, machine settings path, all ALLCLEAR_ env vars in tables and JSON blocks, data directory tree
- docs/hooks.md: "AllClear hooks" product name and four ALLCLEAR_DISABLE_* variables
- docs/architecture.md: directory tree top level, commands/skills note, plugin namespace text
- docs/service-map.md: "AllClear scans", command references, MCP server JSON snippet key and path
- docs/development.md: git clone URL, cd command, plugin-dir path, "/allclear:quality-gate" in Quality Gate section
  </action>
  <verify>
    <automated>grep -rli "allclear\|AllClear\|ALLCLEAR" docs/ && echo "FAIL — allclear references remain in docs/" || echo "PASS — no allclear references in docs/"</automated>
  </verify>
  <acceptance_criteria>
    - grep -ri "allclear" docs/ returns no matches (exit code 1)
    - grep "/ligamen:quality-gate" docs/commands.md returns a match
    - grep "ligamen.config.json" docs/configuration.md returns at least 2 matches
    - grep "~/.ligamen/" docs/configuration.md returns at least 3 matches
    - grep "LIGAMEN_DISABLE_FORMAT" docs/hooks.md returns a match
    - grep "plugin:ligamen" docs/architecture.md returns a match
    - grep "ligamen-impact" docs/service-map.md returns a match
    - grep "chilleregeravi/ligamen" docs/development.md returns a match
  </acceptance_criteria>
  <done>All six docs/ files contain zero allclear/AllClear/ALLCLEAR references; all command invocations, config filenames, env vars, paths, and MCP server names use the Ligamen namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs — PROJECT.md, MILESTONES.md, ROADMAP.md</name>
  <read_first>
    .planning/PROJECT.md
    .planning/MILESTONES.md
    .planning/ROADMAP.md
  </read_first>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md, .planning/ROADMAP.md</files>
  <action>
Update the three planning docs. These are internal planning files — only update occurrences where "allclear"/"AllClear"/"ALLCLEAR" appears as the product name. Do not change historical references to completed v1.0-v3.0 milestones that describe what was built (those are accurate records); only update the product name and command references that identify the tool.

PRODUCT NAME:
- "# AllClear" heading → "# Ligamen" (PROJECT.md top heading)
- "An open-source Claude Code plugin" description line: "AllClear plugin" → "Ligamen plugin"
- "AllClear is" / "AllClear provides" / "AllClear can" → "Ligamen is" / "Ligamen provides" / "Ligamen can"
- "AllClear focused" (in Out of Scope: "keep AllClear focused") → "keep Ligamen focused"
- Constraint line: "allclear.config.json" → "ligamen.config.json"

SLASH COMMANDS IN PROJECT.md (Validated requirements list):
- "/allclear:quality-gate" → "/ligamen:quality-gate"
- "/allclear:cross-impact" → "/ligamen:cross-impact"
- "/allclear:drift" → "/ligamen:drift"
- "/allclear:pulse" → "/ligamen:pulse"
- "/allclear:deploy-verify" → "/ligamen:deploy-verify"
- "/allclear:map" → "/ligamen:map"
- "(plugin:allclear)" → "(plugin:ligamen)"

KEY DECISIONS TABLE (PROJECT.md):
- "Drop /allclear scope" → "Drop /allclear scope" (this is a historical decision label — can leave as-is since it describes a past decision about the old name, OR update to "Drop /allclear scope (renamed to /ligamen)". Use your judgment to keep the table informative.)
- "allclear.config.json" in key decisions table → "ligamen.config.json"
- "Boundary config in allclear.config.json" → "Boundary config in ligamen.config.json"

MILESTONES.md:
- References to "allclear.config.json" → "ligamen.config.json"
- Any standalone product name "AllClear" used as current product name → "Ligamen"
- Historical milestone descriptions that reference "allclear.config.json" as a config file the user interacts with → "ligamen.config.json"

ROADMAP.md:
- "allclear.config.json" (in Phase 39 success criteria) → "ligamen.config.json" (this appears to already be written as the v4.0 goal — verify and update if still says allclear)
- Any remaining "allclear" product name references in phase descriptions → "ligamen"

Do NOT change:
- Historical milestone records about what was accomplished (e.g. "Boundary grouping via allclear.config.json" in v3.0 notes in MILESTONES.md — this is an accurate historical record, leave as-is OR update only the config filename)
- Phase numbers, plan counts, shipping dates
- Git commit messages or references to old behavior that explain what was changed
- The "v4.0 Ligamen Rebrand" milestone text — it's already correct
- STATE.md — not in scope for this task

Practical guidance: In PROJECT.md focus on the top heading, description paragraph, validated requirements list (command names), constraints section (allclear.config.json), and key decisions table. In MILESTONES.md update only config filenames and any "AllClear" product name in non-historical contexts. In ROADMAP.md update any residual allclear product name references in the v4.0 phase descriptions.
  </action>
  <verify>
    <automated>grep -n "allclear\|AllClear" .planning/PROJECT.md .planning/MILESTONES.md .planning/ROADMAP.md | grep -v "v1.0\|v2.0\|v2.1\|v2.2\|v2.3\|v3.0\|shipped\|Shipped\|milestone\|Milestone\|ALLCLEAR" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "# Ligamen" .planning/PROJECT.md returns a match (top heading)
    - grep "/ligamen:quality-gate" .planning/PROJECT.md returns a match
    - grep "plugin:ligamen" .planning/PROJECT.md returns a match
    - grep "ligamen.config.json" .planning/PROJECT.md returns at least 2 matches
    - grep -i "allclear" .planning/PROJECT.md | grep -v "Drop /allclear\|v1.0\|v2.0\|v3.0" returns no matches
  </acceptance_criteria>
  <done>PROJECT.md heading reads "# Ligamen", validated requirements list shows /ligamen:* commands, constraints show ligamen.config.json. MILESTONES.md and ROADMAP.md updated where the product name appears in current-facing context.</done>
</task>

</tasks>

<verification>
Run: grep -ri "allclear" docs/ .planning/PROJECT.md .planning/MILESTONES.md
Expected: No matches (or only historical v1.0-v3.0 references that describe past behavior)

Run: grep "# Ligamen" .planning/PROJECT.md
Expected: Match on line 1

Run: grep "/ligamen:quality-gate" docs/commands.md
Expected: Match

Run: grep "ligamen.config.json" docs/configuration.md
Expected: At least 2 matches
</verification>

<success_criteria>
All docs/ files (commands.md, configuration.md, hooks.md, architecture.md, service-map.md, development.md) reference ligamen exclusively. Planning docs (PROJECT.md, MILESTONES.md) use "Ligamen" as the product name wherever the product name appears.
</success_criteria>

<output>
After completion, create `.planning/phases/44-documentation/44-02-SUMMARY.md` following the summary template.
</output>
