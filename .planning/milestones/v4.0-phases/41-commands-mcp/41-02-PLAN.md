---
phase: 41-commands-mcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp.json
  - worker/mcp/server.js
  - worker/server/chroma.js
autonomous: true
requirements:
  - CMD-02
  - CMD-03

must_haves:
  truths:
    - "The MCP server registers as 'ligamen-impact' — not 'allclear-impact'"
    - ".mcp.json declares the server key as 'ligamen-impact'"
    - "ChromaDB creates/opens a collection named 'ligamen-impact' — not 'allclear-impact'"
    - "No 'allclear-impact' string appears in .mcp.json, server.js, or chroma.js"
  artifacts:
    - path: ".mcp.json"
      provides: "MCP server declaration with new name"
      contains: "ligamen-impact"
    - path: "worker/mcp/server.js"
      provides: "MCP server runtime registration"
      contains: "ligamen-impact"
    - path: "worker/server/chroma.js"
      provides: "ChromaDB collection name constant"
      contains: "ligamen-impact"
  key_links:
    - from: ".mcp.json"
      to: "worker/mcp/server.js"
      via: "Server key 'ligamen-impact' in .mcp.json matches McpServer name in server.js"
      pattern: "ligamen-impact"
    - from: "worker/server/chroma.js"
      to: "worker/mcp/server.js"
      via: "COLLECTION_NAME constant consumed by chroma client, referenced in server.js hint messages"
      pattern: "ligamen-impact"
---

<objective>
Rename the MCP server from "allclear-impact" to "ligamen-impact" in .mcp.json and server.js, and rename the ChromaDB collection constant in chroma.js from "allclear-impact" to "ligamen-impact".

Purpose: Phase 41 CMD-02 and CMD-03 — the MCP server identity and ChromaDB storage namespace must carry the ligamen name so tools registered by the MCP server and embeddings stored in ChromaDB use the correct collection.
Output: .mcp.json, worker/mcp/server.js, and worker/server/chroma.js with all allclear-impact references replaced by ligamen-impact.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MCP server key in .mcp.json</name>
  <files>.mcp.json</files>
  <read_first>.mcp.json</read_first>
  <action>
Current content of .mcp.json:
```json
{
  "mcpServers": {
    "allclear-impact": {
      "type": "stdio",
      "command": "./scripts/mcp-wrapper.sh"
    }
  }
}
```

Replace the server key "allclear-impact" with "ligamen-impact". Write the file as:
```json
{
  "mcpServers": {
    "ligamen-impact": {
      "type": "stdio",
      "command": "./scripts/mcp-wrapper.sh"
    }
  }
}
```

The command path and type are unchanged — only the server registration key changes.
  </action>
  <verify>
    <automated>grep "ligamen-impact" /Users/ravichillerega/sources/allclear/.mcp.json && ! grep "allclear-impact" /Users/ravichillerega/sources/allclear/.mcp.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .mcp.json contains the string "ligamen-impact"
    - .mcp.json does NOT contain the string "allclear-impact"
    - .mcp.json remains valid JSON (parseable by node -e "JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))")
  </acceptance_criteria>
  <done>
    .mcp.json declares MCP server as "ligamen-impact". No "allclear-impact" string present. File is valid JSON.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename MCP server registration in server.js and collection hint in chroma.js</name>
  <files>
    worker/mcp/server.js
    worker/server/chroma.js
  </files>
  <read_first>
    worker/mcp/server.js
    worker/server/chroma.js
  </read_first>
  <action>
In worker/mcp/server.js, make the following targeted replacements:

1. Line with `const server = new McpServer({ name: "allclear-impact", version: "2.0.0" });`
   → Change to: `const server = new McpServer({ name: "ligamen-impact", version: "2.0.0" });`

2. All hint strings that say `"Run /allclear:map to build the dependency map."` or `"Run /allclear:map first in that project"`
   → Replace `/allclear:map` with `/ligamen:map` in these hint strings (4 occurrences at approximately lines 466, 474, 557, 593, 629, 663)

3. The tool description string at approximately line 678:
   `"Trigger a dependency scan via the AllClear HTTP worker. Returns unavailable when the worker is not running."`
   → Change "AllClear" to "Ligamen": `"Trigger a dependency scan via the Ligamen HTTP worker. Returns unavailable when the worker is not running."`

4. The ALLCLEAR_DATA_DIR reference at approximately line 16:
   `process.env.ALLCLEAR_DATA_DIR || path.join(os.homedir(), ".allclear")`
   → Change to: `process.env.LIGAMEN_DATA_DIR || path.join(os.homedir(), ".ligamen")`
   NOTE: This env var and path rename is actually Phase 40 scope. However, server.js has this reference and it must be consistent. Update it here since this is the file being touched, but note in the SUMMARY that ENV-01 and ENV-02 partial coverage occurs here.

In worker/server/chroma.js, make the following targeted replacement:

1. Line 24: `const COLLECTION_NAME = "allclear-impact";`
   → Change to: `const COLLECTION_NAME = "ligamen-impact";`

No other changes in chroma.js — the rest of the file uses the COLLECTION_NAME constant so only the constant definition needs updating.
  </action>
  <verify>
    <automated>grep -n "allclear-impact\|allclear:" /Users/ravichillerega/sources/allclear/worker/mcp/server.js /Users/ravichillerega/sources/allclear/worker/server/chroma.js && echo "FAIL: allclear references remain" || echo "PASS: no allclear-impact or allclear: references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "allclear-impact" worker/mcp/server.js` returns no matches
    - `grep "allclear-impact" worker/server/chroma.js` returns no matches
    - `grep "ligamen-impact" worker/mcp/server.js` returns at least 1 match (the McpServer name line)
    - `grep 'COLLECTION_NAME = "ligamen-impact"' worker/server/chroma.js` returns exactly 1 match
    - `grep "AllClear" worker/mcp/server.js` returns no matches (tool description updated)
  </acceptance_criteria>
  <done>
    worker/mcp/server.js registers MCP server as "ligamen-impact" and all hint strings reference /ligamen:map. worker/server/chroma.js COLLECTION_NAME constant is "ligamen-impact". No "allclear-impact" or "AllClear" strings remain in either file.
  </done>
</task>

</tasks>

<verification>
Final sweep across all 3 files:

```bash
grep -n "allclear-impact\|allclear:" /Users/ravichillerega/sources/allclear/.mcp.json /Users/ravichillerega/sources/allclear/worker/mcp/server.js /Users/ravichillerega/sources/allclear/worker/server/chroma.js
```

Must return no matches.

Validate .mcp.json is valid JSON:
```bash
node -e "JSON.parse(require('fs').readFileSync('/Users/ravichillerega/sources/allclear/.mcp.json','utf8')); console.log('valid JSON')"
```
</verification>

<success_criteria>
1. `grep "allclear-impact" .mcp.json` returns no matches; `grep "ligamen-impact" .mcp.json` returns 1 match
2. `grep "allclear-impact" worker/mcp/server.js` returns no matches; McpServer name is "ligamen-impact"
3. `grep 'COLLECTION_NAME = "allclear-impact"' worker/server/chroma.js` returns no matches; constant is "ligamen-impact"
4. .mcp.json is valid JSON
5. No "AllClear" branding strings remain in worker/mcp/server.js
</success_criteria>

<output>
After completion, create `.planning/phases/41-commands-mcp/41-02-SUMMARY.md` with:
- List of files changed and specific lines modified
- Confirmation of COLLECTION_NAME value in chroma.js
- Note about partial ENV-01/ENV-02 coverage (ALLCLEAR_DATA_DIR renamed in server.js)
- Verification command output showing zero allclear-impact references
</output>
