---
phase: 41-commands-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/quality-gate.md
  - commands/map.md
  - commands/cross-impact.md
  - commands/drift.md
  - commands/pulse.md
  - commands/deploy-verify.md
  - skills/quality-gate/SKILL.md
  - skills/impact/SKILL.md
autonomous: true
requirements:
  - CMD-01
  - CMD-04

must_haves:
  truths:
    - "No /allclear:* string appears in any commands/ or skills/ file"
    - "All 6 command files have description: fields referencing /ligamen:* invocation"
    - "Both SKILL.md files reference /ligamen:* in their description and body text"
    - "The quality-gate result block header says 'Ligamen Quality Gate Results'"
    - "The quality-gate fix result block header says 'Ligamen Fix Results'"
  artifacts:
    - path: "commands/quality-gate.md"
      provides: "Renamed quality-gate command"
      contains: "/ligamen:quality-gate"
    - path: "commands/map.md"
      provides: "Renamed map command"
      contains: "/ligamen:map"
    - path: "commands/cross-impact.md"
      provides: "Renamed cross-impact command"
      contains: "/ligamen:cross-impact"
    - path: "commands/drift.md"
      provides: "Renamed drift command"
      contains: "/ligamen:drift"
    - path: "commands/pulse.md"
      provides: "Renamed pulse command"
      contains: "/ligamen:pulse"
    - path: "commands/deploy-verify.md"
      provides: "Renamed deploy-verify command"
      contains: "/ligamen:deploy-verify"
    - path: "skills/quality-gate/SKILL.md"
      provides: "Renamed quality-gate skill"
      contains: "/ligamen:quality-gate"
    - path: "skills/impact/SKILL.md"
      provides: "Renamed impact skill"
      contains: "/ligamen:map"
  key_links:
    - from: "commands/quality-gate.md"
      to: "skills/quality-gate/SKILL.md"
      via: "Consistent /ligamen:quality-gate references in both files"
      pattern: "/ligamen:quality-gate"
    - from: "commands/map.md"
      to: "skills/impact/SKILL.md"
      via: "Consistent /ligamen:map references in both files"
      pattern: "/ligamen:map"
---

<objective>
Rename all 6 slash commands from /allclear:* to /ligamen:* and update both skill descriptions to reference ligamen naming.

Purpose: Phase 41 CMD-01 and CMD-04 — every user-facing command invocation path and skill auto-trigger prompt must say /ligamen:* so Claude Code routes to the correct renamed commands.
Output: 6 command .md files and 2 skill SKILL.md files with all allclear references replaced by ligamen.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename /allclear:* to /ligamen:* in all 6 command files</name>
  <files>
    commands/quality-gate.md
    commands/map.md
    commands/cross-impact.md
    commands/drift.md
    commands/pulse.md
    commands/deploy-verify.md
  </files>
  <read_first>
    commands/quality-gate.md
    commands/map.md
    commands/cross-impact.md
    commands/drift.md
    commands/pulse.md
    commands/deploy-verify.md
  </read_first>
  <action>
In each of the 6 command files, replace every occurrence of the following strings — in frontmatter description fields, in body text, in code blocks, in usage examples, in result blocks:

1. `/allclear:quality-gate` → `/ligamen:quality-gate`
2. `/allclear:map` → `/ligamen:map`
3. `/allclear:cross-impact` → `/ligamen:cross-impact`
4. `/allclear:drift` → `/ligamen:drift`
5. `/allclear:pulse` → `/ligamen:pulse`
6. `/allclear:deploy-verify` → `/ligamen:deploy-verify`

Also rename the user-facing product name string "AllClear" in result section headers:
- In quality-gate.md line "## AllClear Quality Gate Results" → "## Ligamen Quality Gate Results"
- In quality-gate.md line "## AllClear Fix Results" → "## Ligamen Fix Results"
- In quality-gate.md fix block "Re-run `/allclear:quality-gate`" → "Re-run `/ligamen:quality-gate`"
- In map.md "AllClear Map — Service Dependency Scanner" → "Ligamen Map — Service Dependency Scanner"
- In pulse.md "# AllClear Pulse" → "# Ligamen Pulse"
- In deploy-verify.md "# AllClear Deploy Verification" → "# Ligamen Deploy Verification"

Also replace config file references in commands that reference allclear.config.json — these belong to Phase 40 (env/paths), but the commands also refer to this filename in their body:
- In cross-impact.md and map.md: `allclear.config.json` → `ligamen.config.json`
- This is within scope as these are the user-facing command instruction strings

Note: Do NOT rename ~/.allclear/ path references in commands — those are Phase 40 (env/paths) scope.
Do NOT rename ALLCLEAR_* env vars in commands — also Phase 40 scope.

After editing each file, verify no `/allclear:` strings remain.
  </action>
  <verify>
    <automated>grep -rn "/allclear:" /Users/ravichillerega/sources/allclear/commands/ && echo "FAIL: allclear: references remain" || echo "PASS: no /allclear: references"</automated>
  </verify>
  <done>
    All 6 command files contain zero /allclear:* strings. Every command invocation example shows /ligamen:*. grep -rn "/allclear:" commands/ returns no matches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update skill descriptions to reference ligamen</name>
  <files>
    skills/quality-gate/SKILL.md
    skills/impact/SKILL.md
  </files>
  <read_first>
    skills/quality-gate/SKILL.md
    skills/impact/SKILL.md
  </read_first>
  <action>
In skills/quality-gate/SKILL.md:
- Frontmatter description field: Replace "run /allclear:quality-gate" → "run /ligamen:quality-gate"
- Body: Replace "the `/allclear:quality-gate` command" → "the `/ligamen:quality-gate` command"
- Body: Replace all other /allclear:quality-gate occurrences with /ligamen:quality-gate
- Body: Replace "AllClear" product name references → "Ligamen" (e.g., "AllClear experience" → "Ligamen experience")

In skills/impact/SKILL.md:
- Frontmatter description field: Replace "run /allclear:map" → "run /ligamen:map"
- Body: Replace "the `/allclear:map` command" → "the `/ligamen:map` command"
- Body: Replace "AllClear" product name references → "Ligamen" (e.g., "AllClear service dependency graph" → "Ligamen service dependency graph")
- Body: Replace "allclear-impact" in the MCP JSON example block → "ligamen-impact"
- Body: Replace "~/.allclear/settings.json" → "~/.ligamen/settings.json"
- Body: Replace "ALLCLEAR_CHROMA_MODE", "ALLCLEAR_CHROMA_HOST", "ALLCLEAR_CHROMA_PORT" env var names → "LIGAMEN_CHROMA_MODE", "LIGAMEN_CHROMA_HOST", "LIGAMEN_CHROMA_PORT"

After editing, verify no "allclear" (case-insensitive) strings remain in either SKILL.md.
  </action>
  <verify>
    <automated>grep -rin "allclear" /Users/ravichillerega/sources/allclear/skills/ && echo "FAIL: allclear references remain" || echo "PASS: no allclear references"</automated>
  </verify>
  <done>
    Both SKILL.md files reference /ligamen:* in description frontmatter and body text. No "allclear" string (case-insensitive) appears in either skill file. grep -rin "allclear" skills/ returns no matches.
  </done>
</task>

</tasks>

<verification>
Run final sweep across all touched files:

```bash
grep -rin "allclear" /Users/ravichillerega/sources/allclear/commands/ /Users/ravichillerega/sources/allclear/skills/
```

Must return no matches.

Also verify positive presence of ligamen branding:
```bash
grep -l "/ligamen:" /Users/ravichillerega/sources/allclear/commands/*.md | wc -l
```
Must return 6.
</verification>

<success_criteria>
1. `grep -rn "/allclear:" commands/` returns no matches
2. `grep -rin "allclear" skills/` returns no matches
3. All 6 command files have `/ligamen:` in their description frontmatter field
4. quality-gate.md result headers read "Ligamen Quality Gate Results" and "Ligamen Fix Results"
5. skills/impact/SKILL.md MCP JSON block shows "ligamen-impact" as server name
</success_criteria>

<output>
After completion, create `.planning/phases/41-commands-mcp/41-01-SUMMARY.md` with:
- What was changed (list of files, count of replacements)
- Verification command output confirming zero allclear references
- Any edge cases encountered
</output>
