---
phase: 21-integration-config
plan: 04
type: execute
wave: 2
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - tests/integration/impact-flow.bats
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04
  - INTG-05
  - INTG-06

must_haves:
  truths:
    - "Integration tests cover the complete flow: full scan -> user confirmation -> SQLite persist -> impact query returns correct transitive results"
    - "Integration tests cover incremental scan: only changed-file findings are returned when git state shows one changed file"
    - "Integration tests verify ChromaDB fallback: with ChromaDB unavailable, search falls back to FTS5 then SQL"
    - "Integration tests verify session-start worker auto-start: hook starts worker only when impact-map section present"
    - "All integration tests pass without requiring ChromaDB to be running"
  artifacts:
    - path: "tests/integration/impact-flow.bats"
      provides: "End-to-end bats integration test suite for the complete scan-to-query flow"
      contains: "full scan"
  key_links:
    - from: "tests/integration/impact-flow.bats"
      to: "worker/db.js"
      via: "direct SQL verification of persisted findings after mock scan"
      pattern: "sqlite3.*impact-map.db"
    - from: "tests/integration/impact-flow.bats"
      to: "scripts/session-start.sh"
      via: "bats test invoking session-start.sh with mock allclear.config.json"
      pattern: "session-start.sh"
---

<objective>
Create the integration test suite covering the complete scan-to-query flow, incremental scan behavior, ChromaDB fallback chain, and session-hook auto-start. Tests use mock repos and scripted findings to avoid needing real Claude agents or a running ChromaDB instance.

Purpose: The full scan-to-query flow is covered by tests that catch regressions across all Phase 21 integration points.
Output: tests/integration/impact-flow.bats — a new bats test file with E2E integration tests.
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/designs/cross-impact-v2.md

@tests/session-start.bats
@tests/test_helper.bash

<interfaces>
<!-- Test helper pattern from tests/test_helper.bash -->
<!-- setup_test_db() — creates an isolated temp directory with a fresh impact-map.db -->
<!-- teardown_test_db() — removes the temp directory -->
<!-- Follow existing bats patterns: PROJECT_ROOT, BATS_TEST_DIRNAME, mktemp -d -->

<!-- Mock scan findings format (matches Phase 18 output) -->
<!-- { services: [{ name, repo_id, endpoints: [{ path, method, protocol }] }], connections: [...] } -->
<!-- Write findings directly to SQLite using worker/db.js functions for E2E test isolation -->
<!-- Do NOT mock db.js itself — test against real db functions with test data -->

<!-- For incremental scan test: -->
<!-- 1. Create a git repo in tmp dir with two files -->
<!-- 2. Run first scan (writes repo_state with last_scanned_commit = HEAD) -->
<!-- 3. Modify one file, commit it -->
<!-- 4. Verify that incremental scan diff = only the changed file -->
<!-- git is available in the bats environment -->

<!-- For ChromaDB fallback test: -->
<!-- Use search() from query-engine.js with { skipChroma: true } to verify FTS5 tier -->
<!-- Use search() with { skipChroma: true, skipFts5: true } to verify SQL tier -->
<!-- ChromaDB itself does not need to be running — isChromaAvailable() will be false -->

<!-- For session-start auto-start test: -->
<!-- Reuse mock pattern from tests/session-start.bats -->
<!-- Create temp allclear.config.json with "impact-map": {} in a temp CWD -->
<!-- Verify worker_start_background is called (sentinel file pattern) -->

<!-- Node.js availability: node v20+ is available (required by Phase 14+) -->
<!-- sqlite3 CLI: may not be available on all machines — use node for DB verification -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/integration/impact-flow.bats</name>
  <files>tests/integration/impact-flow.bats</files>
  <behavior>
    Test INTG-E2E-01: Full scan flow — write mock findings to db, query transitive impact, verify A->B->C chain returns all intermediate services
    Test INTG-E2E-02: Incremental scan — first scan records repo_state; second scan with one changed file returns only that file's connections; unchanged files return no findings
    Test INTG-E2E-03: ChromaDB fallback chain — search() with skipChroma=true returns FTS5 results; with skipChroma+skipFts5 returns SQL results; both return arrays
    Test INTG-E2E-04: Session-start auto-start — hook with impact-map in config calls worker_start_background; hook without impact-map does not
    Test INTG-E2E-05: Snapshot on rescan — createSnapshot() creates a file; isFirstScan() changes from true to false after first scan persist
    Test INTG-E2E-06: First-run recommendation — isFirstScan() returns true before writeScan(), false after; SKILL.md contains recommendation text
  </behavior>
  <action>
    Create tests/integration/ directory and tests/integration/impact-flow.bats.

    File structure:
    ```
    #!/usr/bin/env bats
    # tests/integration/impact-flow.bats
    # Integration tests for Phase 21: session hook, ChromaDB fallback, snapshots, E2E scan flow
    # Requires: Node.js 20+, git, bats-core
    # Does NOT require: ChromaDB running, real Claude agents

    PROJECT_ROOT="$(cd "$BATS_TEST_DIRNAME/../.." && pwd)"

    setup() {
      TEST_DIR="$(mktemp -d)"
      export TEST_DIR
    }

    teardown() {
      [[ -d "$TEST_DIR" ]] && rm -rf "$TEST_DIR"
    }
    ```

    Test INTG-E2E-01 (transitive impact query):
    - Use node -e to call db.writeScan() with synthetic findings: service A->B->C chain
    - Then call query-engine.js queryTransitive('A') and assert B and C are in results
    - Assert cycle detection: add C->A connection, re-query, still returns without hanging

    Test INTG-E2E-02 (incremental scan):
    - Init git repo in TEST_DIR, commit two files (service-a.ts, service-b.ts)
    - Call db.writeRepoState() with HEAD commit hash
    - Make a new commit changing only service-a.ts
    - Call scan-manager.getChangedFiles(repoPath, lastCommit) and assert only service-a.ts returned
    - Assert service-b.ts is NOT in changed files

    Test INTG-E2E-03 (search fallback chain):
    - Write a service named 'auth-gateway' to the test DB
    - Call search('auth-gateway', { skipChroma: true }) via Node — assert result contains auth-gateway (FTS5 tier)
    - Call search('auth-gateway', { skipChroma: true, skipFts5: true }) — assert result contains auth-gateway (SQL tier)
    - Both return arrays (not errors)

    Test INTG-E2E-04 (session-start auto-start):
    Reuse the mock pattern from session-start.bats:
    - Create MOCK_PLUGIN_ROOT with mock lib/worker-client.sh (sentinel file on worker_start_background)
    - Create allclear.config.json WITH "impact-map": {} in TEST_DIR
    - Run session-start.sh with CWD=TEST_DIR
    - Assert sentinel file /tmp/allclear_test_intg_worker_started exists (worker was started)
    - Run again with config WITHOUT "impact-map" key
    - Assert sentinel file does NOT exist

    Test INTG-E2E-05 (snapshot on rescan):
    - Write mock findings to DB (first scan)
    - Assert isFirstScan() returns false after writeScan
    - Call createSnapshot('test')
    - Assert snapshot .db file exists in snapshots/ dir
    - Assert map_versions table has one row

    Test INTG-E2E-06 (first-run recommendation in SKILL.md):
    - grep for 'ALLCLEAR_CHROMA_MODE' in skills/impact/SKILL.md
    - grep for 'mcp-server.js' in skills/impact/SKILL.md
    - Both must be present

    For Node.js-based assertions, use the pattern:
    run node --input-type=module --eval "..."
    [ "$status" -eq 0 ]

    For shell assertions, use standard bats patterns with [ ] checks.

    All tests must be hermetic (isolated temp dirs, cleanup in teardown).
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/allclear && ./tests/bats/bin/bats tests/integration/impact-flow.bats 2>&1</automated>
  </verify>
  <done>All 6 INTG-E2E-* tests pass; no external services required; tests complete in under 60 seconds</done>
</task>

</tasks>

<verification>
1. ./tests/bats/bin/bats tests/integration/impact-flow.bats — all tests green
2. INTG-E2E-01: transitive query A->B->C returns B and C; cyclic graph does not hang
3. INTG-E2E-02: incremental scan returns only changed files (git-based)
4. INTG-E2E-03: search() tier 2 (FTS5) and tier 3 (SQL) both return results with skipChroma/skipFts5
5. INTG-E2E-04: session-start auto-starts worker with impact-map config; skips without it
6. INTG-E2E-05: createSnapshot() creates file; isFirstScan() false after writeScan()
7. INTG-E2E-06: SKILL.md contains ChromaDB and MCP recommendation text
</verification>

<success_criteria>
- tests/integration/impact-flow.bats created with 6 integration tests
- All tests pass without ChromaDB running or real Claude agents
- E2E flow covered: full scan persist -> transitive impact query returns correct results
- Incremental scan covered: only changed files trigger findings
- ChromaDB fallback covered: each tier reachable independently
- Session hook auto-start covered: worker called with impact-map, skipped without
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-config/21-04-SUMMARY.md`
</output>
