---
phase: 21-integration-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/db.js
  - skills/impact/SKILL.md
autonomous: true
requirements:
  - INTG-05
  - INTG-06

must_haves:
  truths:
    - "When a user runs /allclear:map for the first time, after findings persist the output recommends configuring ChromaDB and adding the MCP server"
    - "When a re-scan happens and the user opts in to history, a SQLite snapshot is created before the current map is overwritten"
    - "Snapshots use VACUUM INTO for consistency — not cp — and are stored in the per-project snapshots/ directory"
    - "Snapshot count is capped at the configured retention limit (default 10) after every snapshot creation"
  artifacts:
    - path: "worker/db.js"
      provides: "createSnapshot(label) method using VACUUM INTO, snapshot retention cleanup"
      contains: "VACUUM INTO"
    - path: "skills/impact/SKILL.md"
      provides: "First-run recommendation text for ChromaDB + MCP server setup"
      contains: "ALLCLEAR_CHROMA_MODE"
  key_links:
    - from: "worker/db.js"
      to: "snapshots/ directory"
      via: "VACUUM INTO path/to/snapshot.db"
      pattern: "VACUUM INTO"
    - from: "worker/db.js"
      to: "map_versions table"
      via: "INSERT into map_versions after successful VACUUM INTO"
      pattern: "map_versions"
---

<objective>
Implement SQLite snapshot-on-rescan (INTG-06) and the first-map-build post-run recommendations (INTG-05). Snapshots use VACUUM INTO for atomicity, are stored in the per-project snapshots directory, and are subject to a retention cap. The first-run recommendations are injected into the impact skill's SKILL.md output section so /allclear:map surfaces them naturally.

Purpose: Map versioning gives users a safety net before overwriting their graph on re-scan. First-run recommendations help users unlock the full ChromaDB + MCP integration.
Output: Modified worker/db.js (createSnapshot method), updated skills/impact/SKILL.md (first-run recommendation block).
</objective>

<execution_context>
@/Users/ravichillerega/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ravichillerega/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/designs/cross-impact-v2.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- From worker/db.js (Phase 14) -->
<!-- DB path: ~/.allclear/projects/<hash>/impact-map.db -->
<!-- Snapshot dir: ~/.allclear/projects/<hash>/snapshots/ -->
<!-- map_versions table schema (Phase 14): -->
<!--   id INTEGER PRIMARY KEY, created_at TEXT, label TEXT, snapshot_path TEXT -->
<!-- VACUUM INTO 'path/to/snapshot.db' — atomic consistent copy (not cp) -->
<!-- Snapshot path stored relative to db file location (not absolute) -->
<!--   Relative: snapshots/2026-03-15T12:00:00.db -->
<!--   Resolved at read time: path.join(path.dirname(dbPath), snapshot_path) -->

<!-- Retention policy: -->
<!--   Default limit: 10 snapshots -->
<!--   Configurable via allclear.config.json "impact-map": { "history-limit": 10 } -->
<!--   After each snapshot creation: SELECT snapshot_path FROM map_versions ORDER BY created_at DESC LIMIT -1 OFFSET historyLimit -->
<!--   Delete the returned rows from filesystem + map_versions table -->

<!-- Pitfall 12: Snapshot bloat (PITFALLS.md) -->
<!-- Solution: retention cleanup after every snapshot, .allclear/ in .gitignore -->

<!-- First-run recommendations (INTG-05) -->
<!-- Triggered when: map_versions count was 0 before this scan (first successful map build) -->
<!-- Surfaced via: skills/impact/SKILL.md output section describing what to do after first map -->
<!-- The /allclear:map command (Phase 20 SKILL.md) reads this and outputs the recommendation -->
<!-- Recommendation content: -->
<!--   1. Configure ChromaDB: add ALLCLEAR_CHROMA_MODE=local to ~/.allclear/settings.json -->
<!--   2. Add MCP server: add allclear-impact to .mcp.json at project root -->
<!--   The MCP server .mcp.json block is shown verbatim for copy-paste -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createSnapshot() and retention cleanup to worker/db.js</name>
  <files>worker/db.js</files>
  <behavior>
    - createSnapshot(label) creates a timestamped SQLite snapshot using VACUUM INTO
    - Snapshot filename: ISO timestamp + .db, stored in snapshots/ subdirectory relative to DB file
    - After VACUUM INTO succeeds: INSERT into map_versions (created_at, label, snapshot_path) with relative path
    - Retention cleanup runs after every snapshot: delete oldest snapshots beyond limit, both from map_versions table and from filesystem
    - Default retention limit: 10 (reads from settings or config "history-limit" field)
    - Returns the snapshot path on success, throws on VACUUM INTO failure
    - isFirstScan() helper: returns true if map_versions count is 0 before this scan
  </behavior>
  <action>
    Add to worker/db.js (after existing exports):

    export function createSnapshot(label = '') {
      const ts = new Date().toISOString().replace(/[:.]/g, '-');
      const snapshotsDir = path.join(path.dirname(dbPath), 'snapshots');
      fs.mkdirSync(snapshotsDir, { recursive: true });
      const snapshotFile = path.join(snapshotsDir, ts + '.db');
      const relPath = path.join('snapshots', ts + '.db');

      // VACUUM INTO creates a consistent copy — safe during active writes
      // Unlike cp which copies wal + shm sidecars (potentially inconsistent)
      db.exec(`VACUUM INTO '${snapshotFile}'`);

      // Record in map_versions table
      const insertVersion = db.prepare(
        'INSERT INTO map_versions (created_at, label, snapshot_path) VALUES (?, ?, ?)'
      );
      insertVersion.run(new Date().toISOString(), label, relPath);

      // Retention cleanup: remove oldest snapshots beyond limit
      const limit = getHistoryLimit();
      const toDelete = db.prepare(
        'SELECT id, snapshot_path FROM map_versions ORDER BY created_at DESC LIMIT -1 OFFSET ?'
      ).all(limit);
      for (const row of toDelete) {
        const fullPath = path.join(path.dirname(dbPath), row.snapshot_path);
        try { fs.unlinkSync(fullPath); } catch (_) {}
        db.prepare('DELETE FROM map_versions WHERE id = ?').run(row.id);
      }

      return snapshotFile;
    }

    export function isFirstScan() {
      const row = db.prepare('SELECT COUNT(*) as cnt FROM map_versions').get();
      return (row?.cnt ?? 0) === 0;
    }

    function getHistoryLimit() {
      // Read from config if available, default 10
      try {
        const cfg = JSON.parse(fs.readFileSync(configPath, 'utf8'));
        return cfg['impact-map']?.['history-limit'] ?? 10;
      } catch (_) { return 10; }
    }

    Note: dbPath and configPath are module-level variables already set during db initialization in Phase 14.
    Node.js built-ins path and fs must be imported at top of db.js if not already present.

    The snapshot creation call site is in the scan-manager.js (Phase 18) or command layer (Phase 20):
      if (existingMapData && userWantsHistory) {
        await createSnapshot('pre-rescan');
      }
    The executor for THIS plan should add createSnapshot() and isFirstScan() to db.js exports only.
    Wiring to the call site is already handled by Phase 20 command layer.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/allclear && node --input-type=module --eval "
import { createSnapshot, isFirstScan } from './worker/db.js';
// isFirstScan before any snapshot: true
const first = isFirstScan();
console.assert(first === true, 'isFirstScan must be true before first snapshot');
// createSnapshot creates a file
const snapshotPath = createSnapshot('test');
const { existsSync } = await import('fs');
console.assert(existsSync(snapshotPath), 'snapshot file must exist after createSnapshot');
console.log('PASS: createSnapshot and isFirstScan OK');
"
    </automated>
  </verify>
  <done>worker/db.js exports createSnapshot(label) using VACUUM INTO and isFirstScan(); snapshot written to snapshots/ dir; retention cleanup runs; map_versions table updated</done>
</task>

<task type="auto">
  <name>Task 2: Add first-run recommendation block to skills/impact/SKILL.md</name>
  <files>skills/impact/SKILL.md</files>
  <action>
    Read the existing skills/impact/SKILL.md. Locate the output/results section or the section describing what /allclear:map produces after a successful run.

    Add a "First Map Build" subsection under the results or post-run guidance section:

    ## After First Map Build

    When this is the first successful map build (no prior map_versions), output these recommendations:

    ---
    Map built successfully. To unlock the full AllClear experience:

    **1. Enable semantic search (optional but recommended)**
    Add to ~/.allclear/settings.json:
    ```json
    {
      "ALLCLEAR_CHROMA_MODE": "local",
      "ALLCLEAR_CHROMA_HOST": "localhost",
      "ALLCLEAR_CHROMA_PORT": "8000"
    }
    ```
    Then start ChromaDB: `docker run -p 8000:8000 chromadb/chroma`

    **2. Add AllClear impact checking to all your Claude agents**
    Create or update `.mcp.json` at your project root:
    ```json
    {
      "mcpServers": {
        "allclear-impact": {
          "type": "stdio",
          "command": "node",
          "args": ["${CLAUDE_PLUGIN_ROOT}/worker/mcp-server.js"]
        }
      }
    }
    ```
    After saving, fully restart Claude Code for MCP changes to take effect.
    ---

    The /allclear:map skill reads this section and outputs it verbatim after the first successful persist.
    The check `isFirstScan()` from db.js is called before writeScan() to determine whether to show this.

    Preservation: do NOT remove or alter existing SKILL.md content. Append this section at the end or insert in the post-run section if it exists.
  </action>
  <verify>
    <automated>grep -n "ALLCLEAR_CHROMA_MODE" /Users/ravichillerega/sources/allclear/skills/impact/SKILL.md && grep -n "mcp-server.js" /Users/ravichillerega/sources/allclear/skills/impact/SKILL.md && echo "PASS: first-run recommendations present in SKILL.md"</automated>
  </verify>
  <done>skills/impact/SKILL.md contains ChromaDB setup instructions and .mcp.json registration block in a first-map-build section</done>
</task>

</tasks>

<verification>
1. createSnapshot() creates a .db file in snapshots/ subdir and records in map_versions table
2. isFirstScan() returns true before first snapshot, false after
3. Retention cleanup: creating 11 snapshots leaves exactly 10 (default limit)
4. skills/impact/SKILL.md contains both ChromaDB setup and .mcp.json block
5. VACUUM INTO pattern confirmed (not cp)
</verification>

<success_criteria>
- worker/db.js: createSnapshot(label) uses VACUUM INTO, records relative path in map_versions, cleans up beyond retention limit
- worker/db.js: isFirstScan() returns true before any map_versions rows exist
- skills/impact/SKILL.md: contains first-run recommendation section with ChromaDB config + .mcp.json block
</success_criteria>

<output>
After completion, create `.planning/phases/21-integration-config/21-03-SUMMARY.md`
</output>
